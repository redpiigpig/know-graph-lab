---
name: research-data-buddhology
description: 「當代佛學研究」collection（/research-data/buddhist-studies）—— 二十世紀以來的佛學研究書目，七個策展分區：佛學研究方法論、經典批判與詮釋、教史研究（按地區）、性別研究、社會研究、制度研究、教理研究，另加電子圖書館的館藏盤點。含策展書目的欄位規格、外文比重的硬要求（日德法合計須逾三分之一）、「已入館／缺」比對那兩個相反的壞法、以及館藏其實九成是全集原著這件事。Use when 要補佛學書目、加一區、把缺書倒進 z-lib 獵表、或使用者提到「當代佛學」「佛教研究」「批判佛教」「京都學派」「入世佛教」「人間佛教」「比丘尼」「八敬法」「寺院經濟」「疑偽經」「漢巴對照」。⚠️ 與 [[scripture-tripitaka]] 的分工：那邊收原典，這邊收研究；與 [[research-data-hongshi]] 的分工：那邊收刊物典藏，這邊收學術專著。
---

# 當代佛學研究（/research-data/buddhist-studies）

收二十世紀以來的佛學研究書目。2026-09-11 開卡，是
[[research-data-theology]]「當代神學研究」的佛學版——架構、欄位、腳本命名、頁面結構
一律比照，只換配色（神學 indigo，佛學 amber）。

## 佛光大辭典（2026-09-13 上線）

使用者把《佛光大辭典增訂版【阿彌陀佛】20230501》放進下載區，是三個分卷
（`.zip.001/.002/.003`，54 MB）。**那不是多卷 zip，是單一 ZIP 被逐位元組切三段**
（`.001` 開頭就是 `PK`），`cat` 接起來即可——別去找 7z 的多卷模式。

裡面是 **MDict**：`.mdx` 11.7 MB（釋義）＋`.mdd` 42.3 MB（資源）。標頭雖寫
`Encrypted="2"`、`RegisterBy="EMail"`，但那是 MDict 標準的固定鹽值加擾、不是使用者
綁定的 DRM，`mdict_utils` 直接讀得出來（`readmdict` 硬性要 python-lzo，Windows 上
裝不起來，改用 `mdict_utils`）。

管線：`scripts/fgs_dictionary_ingest.py`（`--unpack/--parse/--images/--r2/--index`）。

| | |
|---|---|
| 詞條 | 32,133（另有一條是辭典自己的說明頁，不收） |
| 釋義 | 漢字 606 萬，最短 8 字以上，零空白 |
| 原書頁碼 | 30,350 條（94.5%），可直接作註腳 |
| 插圖 | 2,968 張；缺字圖 284 張；合計 3,252 張／29.3 MB |
| 正本 | Drive `_corpus/fgs-dictionary/`（src/ 原始檔、images/ 圖、FGS.jsonl 詞條） |
| 服務 | R2 `fgs-dictionary/`，由 `server/api/glossary/image/[name]` 串流（先 R2 後回退 Drive） |

### 🚨 這一批踩過的四個坑，都屬於「看起來完全正常」那一類

1. **有些 `<img>` 不是插圖，是「缺字圖」。**罕用字無法編碼時直接嵌一張小圖當一個字用
   （`䞋` 條的「又作嚫、▢、襯」中間那個就是）。直接 strip 標籤轉純文字會把那個字
   **整個吃掉，而句子依然通順**。現在一律換成 `▢` 並把檔名記進 `glyphs`，頁面上把圖
   貼回去。
2. **原書頁碼有兩種寫法。**主要樣式是 `<span style="color: #942923">p8653</span>`
   （29,574 條），但另有約 800 條是**直接寫在正文尾巴的純文字 `p1`／`p47`**
   （【一】【一休和尚】都是）。只認第一種會靜默少掉 2.4%——同弘誓 157 期分隔符那個坑。
3. **條數別拿 1988 年八冊初版的「約 22,600 條、360 餘萬字」對帳。**這個檔是十冊
   增訂版，自報「三萬餘條、近三千幀圖表、近千萬言」——32,134 條／3,264 張圖／
   823 萬字元三項都對得上。拿錯版本會以為漏抓三分之一。
4. **`.mdd` 不要照單全收。**裡面混著 iOS app 的啟動畫面與圖示（38 張、4.1 MB）、
   `fgsdict.s3db`（50 MB SQLite）、33 個 `fgsdict_bookN.xml`。現在改成**只收詞條真的
   引用到的圖**，順便讓「引用了卻抽不到」的破圖在抽圖那一步就現形（實測 0 張）。

### ⚠️ 授權：與法鼓那十二部不同，別混為一談

使用者 2026-09-11 表示已取得佛光山授權。但本站所據的這個檔是**網路流傳的 MDict 版，
由佛光山官方 app 拆包製成**（`.mdd` 裡的 `fgsdict.s3db` 與 iOS entitlements 是證據），
不是佛光山交付的授權資料檔。索引裡 `rights: by-permission`，`license` 欄照實寫明這件事，
**對外開放前須向佛光山確認授權範圍**。

⚠️ 載入器 `server/utils/glossaries.ts` 現在讀**兩個**目錄（`dila-glossaries` 與
`fgs-dictionary`）。目錄分開就是為了不讓授權不同的兩批混在一起，新增第三部時照這個規矩。

## 🚨 2026-09-12 華語候選清單的抽樣複核：精確率從 45% 到 100%

每區隨機抽 20 筆逐筆判讀，數字寫進 `buddhist_studies_articles.py` 的 `PRECISION`
並印在頁面上。**成果不是「清單變乾淨」而是「讀者知道這份清單有多不乾淨」**，
所以假命中一律不刪，只把低精確率的詞列進 `WEAK`，並在頁面另列一區。

| 100% | 90–85% | 80–75% | 45% |
|---|---|---|---|
| 教理研究、性別研究 | 方法論 90、教史 85 | 社會 80、制度 75 | **經典批判與詮釋** |

弱詞（實測）：

* 🚨 **`阿含`**（經典批判與詮釋）——本區要的是文獻學（校勘、對勘、寫本），而這個詞
  會把導讀、選集序、研修營側記、講記摘編全部撈進來。20 筆抽樣裡 11 筆是這類，
  本區精確率因此只有 45%。152 筆裡 61 筆只靠弱詞命中。
* `慈善`（社會研究）——命中「花蓮**慈善寺**」，那是寺名。
* `居士`、`道場`（制度研究）——多半是稱謂（「覆清泉居士」）與修辭／圖像學名目
  （「人間道場」「毗盧道場」），不是居士制度或道場制度。
* `傳播`（教史研究）——命中「大愛電視的傳播理念」這類傳播學文章。

### ⚠️ 社會研究那一區的問題不是弱詞，是語料本身

該區 391 筆裡絕大多數命中的是 `人間佛教`，因為所掃的刊以弘誓與佛光系統為主，
而「人間佛教」是它們的**自我標籤**而不是一個社會議題——所以抽樣中的假命中
（禪七法談、治心十法、禪學理念）都是「人間佛教掛名的非社會研究」。
這不能靠標弱詞解決（標了會把八成正確的篇目一起標掉），要靠**擴充刊物來源**。

## 🚨 與別張卡片的邊界

| 材料 | 歸哪裡 |
|---|---|
| 大正藏／卍續藏／漢譯南傳的**經律論原文** | [[scripture-tripitaka]]（/tripitaka，3,784 部全文） |
| 二十世紀以來學者寫的**研究專著** | **本卡片** |
| 弘誓雙月刊、學團日誌、玄奘佛學研究學報等**刊物典藏** | [[research-data-hongshi]] |
| 中華佛學學報、法鼓、臺大佛學研究等**期刊篇目與卷期頁碼** | [[research-data-airiti]]（卷期頁碼只有華藝有） |
| 印順、太虛、聖嚴的**全集** | `/collected-works`（見 [[ebook-collected-works]]），不算圖書館館藏 |

判準跟神學那條線一樣：**原典 → 經典對照工具；研究 → 研究資料卡片。**

## 七區與現況（2026-09-11 第一輪）

| 區 | slug | 筆數 | 已入館 |
|---|---|---|---|
| 佛學研究方法論 | `method` | 29 | 0 |
| 經典批判與詮釋 | `textual` | 42 | 2 |
| 教史研究（帶 region） | `history` | 46 | 4 |
| 性別研究 | `gender` | 24 | 0 |
| 社會研究 | `social` | 26 | 2 |
| 制度研究 | `institution` | 23 | 1 |
| 教理研究 | `doctrine` | 58 | 4 |
| **合計** | | **248** | **13（5.2%）** |
| 館藏佛學 | `library` | 32 本／3,103 段（另有全集 283 卷） | — |

`history` 的 `region` 是**地理軸**：印度 15／中國 7／東南亞 7／近代改革 7／藏傳 4／
日本 3／韓國 3。與神學那張的 `region`（處境軸，「黑人神學」與「亞洲‧台灣」並列）不同，
不要互相套用。

## 🚨 語言比重是硬要求，不是「有就好」

使用者明確交代過（並見 `feedback_dazangjing_diversity`「越近代越不可西方中心」）：

```
日 + 德 + 法 合計 ≥ 全部書目的 1/3，且每一種單獨 ≥ 10 筆
英文不得佔到八成
```

第一輪達成：**英 113（46%）／日 55（22%）／中 32（13%）／法 24（10%）／德 23（9%）／韓 1**，
日德法合計 102 筆＝41%。補書目時要維持這個比例，不能一路補英文把它稀釋掉。

三條學術傳統各自不可缺席的名字：

* **日文**——中村元、宇井伯壽、平川彰、高崎直道、藤田宏達、下田正弘、柳田聖山、
  望月信亨、牧田諦亮、赤沼智善、水野弘元、辛嶋靜志、船山徹、佐佐木閑；
  批判佛教那條線**三人都要**（松本史朗、袴谷憲昭、伊藤隆壽）。
* **德文**——Oldenberg、Lüders、Waldschmidt、Frauwallner、Schmithausen、Schlingloff、
  von Hinüber、Bechert、Geiger、Steinkellner。哥廷根的兩套研討會論文集
  （最古聖典的語言、佛陀年代判定）是這一系的代表。
* **法文**——Burnouf、Lévi、La Vallée Poussin、Przyluski、Lamotte、Demiéville、
  Bareau、Mus、Filliozat、Gernet、Strickmann、Faure。法國在中觀、律藏、
  敦煌與禪宗史上份量極重，缺了這一塊書目會歪。

⚠️ **日文書名有漢字就照原漢字寫在 `title`，不要另譯**（`title_zh` 放繁體化的同一書名，
の→之／的）。只有純假名書名才另譯，例如「出家とはなにか」→《何謂出家》。
見 `feedback_translation_register_and_titles`。

⚠️ **寧可不收也不要編造。** 日文與法文著作的年份與書名特別容易記錯，拿不準就換一本
確定的。`zh`（中譯本）欄尤其——只在確知有中譯本時才填，第一輪只填了七筆
（韋伯、沃德爾、許理和、末木文美士、舒馬赫、德容、謝和耐）。

## 書目的欄位

`data/research-data/buddhist-studies/bibliography.jsonl`，一行一筆：

```json
{"area":"gender","author":"Shi Chaohwei 釋昭慧","author_zh":"釋昭慧","year":2002,
 "title":"千載沉吟——新世紀的佛教女性思維","title_zh":"千載沉吟——新世紀的佛教女性思維",
 "lang":"zh","note":"二〇〇一年廢除八敬法運動的理論文本","zh":"（有中譯本時才填）"}
```

* `area` ∈ method / textual / history / gender / social / institution / doctrine
* `region` 只有 history 用
* `lang` ∈ en / ja / zh / fr / de / ko
* `author` 寫原文姓名，中日韓學者用「羅馬字 漢字」並列（比對只取最後一段，所以漢字要在後面）
* `author_zh` 是比對的關鍵欄，**寫最短的通行形**：印順而不是釋印順／印順法師，
  否則對不上館內的兩種寫法
* `in_library` 是跑腳本時算出來的，**不要寫進 jsonl**
* 不可有重複的 (`author_zh`, `title_zh`)

## 🚨 「已入館／缺」比對有兩個相反的壞法

`scripts/buddhist_studies_index.py` 的 `in_library()`。兩種錯都不會報錯：

1. **只拿原文題名比** → 得出「248 本幾乎全缺」。因為館內題名九成是中文，
   `Histoire du bouddhisme indien` 當然對不上《印度佛教史》。後果是把已經有的書
   全倒進獵表重抓。
2. **只拿題名比** → 拉莫特、平川彰、沃德爾、聖嚴四本《印度佛教史》被判成同一本；
   館裡光《中國佛教史》就有蔣維喬與鎌田茂雄兩種。後果是以為有而其實沒有。

現行作法：題名（原文與中譯都比）命中之後，**還要作者對得上**才算；作者欄空白的
只有在題名夠獨特（八字以上）時才採信。取寧可漏報的一側。

### 比神學那支多做的一件事：切破折號

`key_words()` 的分隔字元多了 `—` 與 `–`。日文書名慣用
「日本仏教史——思想史としてのアプローチ」這種長副標，館內中譯本卻只叫《日本佛教史》，
不切破折號整批對不上（實測：切了之後末木文美士那本才命中）。

### 🚨 命中率 5.2% 是實情，不是故障

第一輪 248 筆只有 13 筆在館。動手「修」之前先看這三件事：

* 館內 `category='世界宗教' AND subcategory='佛教'` 共 **313 筆，其中 282 筆是
  `collection='collected-works'`**——印順、太虛、聖嚴三套全集的個別卷。那是**原著不是研究**，
  且依 `feedback_collected_works_not_in_library` 本來就不算圖書館館藏。
  真正的二手研究**只有 31 本**（加上觀音信仰史那一本共 32）。
* 那 31 本逐本核過了，命中的 13 筆全是真的（印順六筆、湯用彤、蔣維喬、沃德爾、
  末木文美士、卜正民、Madsen 各一）。沒有漏抓。
* 所以低命中率反映的是**館藏真的很薄**，這正是本卡片以策展書目為主的原因。

判斷比對是否壞掉的實際方法：拿「館內確定有的那 31 本」裡與書目重疊的幾筆去驗，
不要看整體百分比。

## 館藏區用了哪些桶

```python
LIBRARY_BUCKETS = [("世界宗教", "佛教"), ("世界宗教", "佛教/觀音信仰史"), ("佛學", None)]
```

這是 2026-09-11 實際查 `ebooks` 表得出的，**不是猜的**。查證時發現的事：

* 世界宗教底下與佛教相關的 subcategory 只有這兩個。
* 另有一本被歸在 category `佛學`（subcategory `佛教倫理學`）——昭慧法師《心靈的交會》，
  是全集那一套，孤例。
* **「東亞宗教」那 42 本不收**：逐本看過，幾乎全是儒教與道教（道教簡史、朱熹的歷史世界、
  製造儒家…），只有《佛教與儒教》一本沾邊。用 subcategory 名字猜會收進一堆不相干的。

`library_stats()` 會把全集與一般館藏**分開報**，頁面上全集那幾格用紫色標示。
混在一起講「館藏三百本佛學研究」是灌水。

## 館藏資料本身的兩個坑

* `ebooks.author` 有 13% 放的是書名、11% 空白（見 [[research-data-theology]]），
  任何靠作者佐證的比對都會低估。
* **館內有錯字**：《如來藏思想》的作者被打成「高崎宣道」，正確是**高崎直道**。
  書目寫正確名字，於是這本對不上（判成「缺」）。這是正確的取捨——不要為了讓數字好看
  去把書目改成錯的。要治本得清 `ebooks.author`。

## 建置

```bash
python scripts/buddhist_studies_index.py            # 建索引
python scripts/buddhist_studies_index.py --wanted   # 另外吐出缺書獵表
```

輸出 `public/content/research-data/buddhist-studies/index.json`（進版控），
缺書獵表寫到 `data/zlib-wanted/buddhist-studies.jsonl`，交
[[ebook-zlib-harvest]] 的每日排程去找。

### 🚨 獵表這個檔名早就有人在用

`data/zlib-wanted/buddhist-studies.jsonl` **在本卡片開卡前就存在**——
[[ebook-zlib-harvest]] 手工策展過 22 筆 `bs-*`（威廉斯、格辛、卡魯帕哈那、
戈麥斯《極樂國土》等）。第一次跑 `--wanted` 直接覆寫，把那 22 筆默默刪掉且毫無錯誤訊息，
是靠 `git status` 顯示 `M` 而非 `A` 才發現的。現在 `--wanted` 改成**合併**：
本輪產生的照新的寫，本輪沒產生到的舊 key 原樣保留。日後在別的卡片複製這支腳本時
要記得檢查獵表檔名有沒有撞到。

### 🚨 獵表的 key 對中日文書名會整批互撞

`key` 的 slug 只留 `a-z0-9`，日文與中文書名 slug 完之後**一律是空字串**，
於是全退到「區-年份」這個鍵上互相覆蓋——實測撞掉 11 筆，而且不會報錯，
只是獵表默默少了幾本。現在的退路是接作者漢字（`bs-method-1989-袴谷憲昭`），
再撞才補流水號。神學那支腳本有同樣的結構，但因為書目幾乎全是西文所以沒發作。

頁面：`pages/research-data/buddhist-studies/index.vue`（七區＋館藏），
卡片掛在 `pages/research-data/index.vue`。

⚠️ PostgREST 沒帶 limit 會**靜默截在 1000 筆**（見 `feedback_postgrest_silent_1000_cap`），
`_paged()` 一定要用，別改成單次查詢。

## 還沒做的事

📋 這一節每一項都有一則可直接貼給新 session 的 prompt，見 [docs/PROMPTS_religious_studies_backlog_2026-09-12.md](../../../docs/PROMPTS_religious_studies_backlog_2026-09-12.md)（含背景、坑、驗收條件）。

1. ~~文章那一層~~ **華語那半已完成**（2026-09-11）：`buddhist_studies_articles.py`
   掃十三份華藝佛學期刊 4,502 篇，七區篩出 1,487 筆候選，97% 華藝有全文。
   ⚠️ 是關鍵詞粗篩不是分類，**未經人工複核**。
   **外文那半還沒有**：*JIABS*、*Journal of Buddhist Ethics*（全開放取用，
   值得優先做）、*Buddhist Studies Review* 要另外評估。
2. **中譯資訊多半空著。** `zh` 只填了七筆，其餘要逐筆查證。台灣的華宇出版社
   「世界佛學名著譯叢」（100 冊）是最大宗的日文與西文佛學名著中譯來源，
   值得整套比對回填。
3. **還缺的語種**：韓文只有一筆（金煐泰），藏文與巴利／僧伽羅文的當地學術著作全無。
   越南、蒙古的佛教研究也還沒收。
4. **書目還可以更厚的幾塊**：南傳阿毘達磨、天台與華嚴的西文研究、
   日本近代佛教與戰爭責任、中國大陸 1980 年代以後的佛教研究、
   以及佛教與科學／心理學這一新興領域。
5. **與 /tripitaka 的互指還沒接起來**：某部經的 reader 頁應該能連到本卡片相關的研究，
   反向亦然。
