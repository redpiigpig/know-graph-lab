---
name: research-data-christianity
description: /research-data 底下三個基督教相關 collection 的抓取與上架流程 — 台灣基督長老教會（教會公報新聞網、新使者、總會重要文獻、賴永祥本土信徒傳記）、台灣福音派（基督教論壇報）、無教會主義（研究文獻＋《無境界者》雜誌，中日雙語）。三者合起來是博士論文第四章「長老教會的公共神學發展」與第六章「議題結盟」的史料底本，並與佛教側刊物一起進 /research-data/corpus 語料層做逐年對照。Use when 要補抓／重抓任一刊物、加新來源、調子頁、處理這些站台的解析陷阱。與 [[research-data-hongshi]]（佛教側）並列於同一 portal。
---

> ⚙️ 所有中文一律繁體（[[feedback_traditional_chinese_only]]）。抓取一律單執行緒＋延遲；義務維護的個人史料庫（laijohn）放到 0.5 秒。

# 三個基督教 collection

`/research-data`（`middleware:'auth'` 需登入）。記憶：[[project_pct_research_data]]、[[project_evangelical_collection]]、[[project_mukyokai_collection]]。

| collection | slug | 子頁 | 現況 |
|---|---|---|---|
| 台灣基督長老教會 | `pct` | 教會公報新聞網 `/tcnn` | 38,451 篇 / 4,730 萬字（2010-12–） |
| | | 新使者 `/new-messenger` | 176 期 3,848 篇 |
| | | 總會重要文獻 `/documents` | 121 件（1971–2015，含三大聲明與 1985 信仰告白） |
| | | 本土信徒傳記 `/laijohn` | 2,078 人 4,221 篇 |
| | | 白話字教會公報 `/poj` | **2,735 篇 / 344 萬字（漢羅）**，1885–1969 |
| 台灣福音派 | `evangelical` | 基督教論壇報 `/ct` | 篇目 23,725，內文 **23,231 全數上架 / 4,66 萬字、署名 100%**（扣掉 494 個專題頁即全收；**實際可用 2019–2026**，2015–2018 合計僅 31 篇） |
| 無教會主義 | `mukyokai` | （單頁） | 研究文獻 13 件（中日雙語）＋《無境界者》十期 159 篇；另有日韓文書目 197 筆／全文 74 篇待上架 |
| 一貫道 | `yiguandao` | 六分頁 | 研究報告／年表／已取得檔案（28 案 3,484 張）／書目 417 筆／解密全文 2 份 12.2 萬字／研究文獻 35 筆（**需登入**）。判讀與交件流程見 [[research-data-yiguandao]] |
| 期刊與報紙 | `press` | （單頁） | 刊物本位的橫切層，標明全文／篇目索引／檢索指引三級 |

R2 前綴：`pct-fulltext/`、`evangelical-fulltext/`、`mukyokai/`＋`mukyokai-fulltext/`。
API：`pct-text`（逐篇 txt）、`pct-tcnn-text`（按年 JSONL，帶 year 回篇目、帶 id 回單篇）、`ct-text`、`mukyokai-text`、`mukyokai-file`（Drive 正本→R2 後備）。

## 🚨 安全：頁面有鎖不等於資料有鎖

2026-09-02 使用者要放一批「勿外傳」的檔案，說「我的網站有密碼」。實測後發現
**兩層假設都是錯的**，兩個都不會報錯：

1. **API 沒有驗證。** 頁面的 `middleware: 'auth'` 只擋頁面。未登入直接打
   `/api/research-data/*` 九支端點全部回 **HTTP 200 連同全文**。
   已補 `requireAdmin`（`server/utils/auth-helper.ts`，照片那批 API 早就在用）。
   前端 `$fetch` 要跟著改 `authedFetch`，否則頁面全壞。
   🚨 `<a href="/api/…&download=1">` 這種直連**帶不了 Authorization header**，
   端點加驗證後會 401 —— 改用 `authedDownload()`（取 blob 再觸發下載）。
2. **repo 是公開的**（`github.com/redpiigpig/know-graph-lab`，visibility=public）。
   把全文放進 `public/content/` 就等於公開流傳，而且 git 歷史刪不掉。
   非公開材料一律：**全文上 R2（`research-private/`），repo 只留書目層**，
   由掛 `requireAdmin` 的端點供應。

放非公開材料前，三層都要實測過：
```
頁面 → 302 導登入 ｜ API → 401 ｜ R2 未簽名 GET → 400（桶無公開讀取）
提交前掃 staged diff：密碼、被監控者姓名、堂號皆 0 次
```

## 國史館檔案（加密 docx）`scripts/archives_guoshiguan.py`

國防部「捕鼠案」與行政院「敵偽組織及活動案（五）」，合計 12.5 萬字，均已註銷機密。
🚨 **檔案局查無「捕鼠案」是對的——它在國史館**，兩個館要分開查。
- 加密 docx 用 `msoffcrypto-tool` 解；密碼走環境變數 `GSG_DOCX_PW`，不進版控
- 🚨 **儲存格順序是反的**。傳統直排表格由右至左讀，`row.cells` 卻由左至右。
  證據：目次那列出來是「31｜30｜29｜28｜27｜**次目**」——數字遞減且「次目」正是
  「目次」反過來。每列都要 `reversed()`
- 🚨 **格內也是直排**，文字逐字換行（`中
華
民
國`），要清掉空白才併得回。
  修完單字元行從 15% 降到 1%
- `位單轉移`／`位單案原` 這種格內橫排右至左的欄位名**原樣保留**——需要語意判斷，
  硬套規則會把正常詞也弄反

## 儲存配置：什麼放哪裡（2026-09-04 盤點）

| | 位置 | 量 |
|---|---|---|
| 報紙期刊全文 | **R2**（gzip JSONL） | 390 MB |
| 檔案局影像 | **Drive**，不上 R2 不上站 | 3.6 GB |
| 照片 | **Drive 原檔**；R2 縮圖已刪 | 71,851 張 |
| 書目／索引 | repo `public/content/` | 小 |
| 非公開材料全文 | R2 `research-private/` ＋ requireAdmin 端點 | — |

🚨 **常見的誤記，查證過的事實**：
- **照片不在 Supabase**，全在 R2（曾佔 6.66 GB）。Supabase 只有 32 KB 的 `pong_photos`
- **報紙全文不在 Supabase**，一開始就是 file-backed。站上索引本來就只有標題日期作者
- **「放回 Drive」不等於搬家**：Zeabur 上的伺服器碰不到本機 `G:`，搬到 Drive＝該功能下線；
  要讓網站還讀得到，只能搬 R2

2026-09-04 刪掉 R2 上 138,674 個照片縮圖，**8.72 GB → 2.06 GB（87%→21%）**。
Drive 原檔完好，`sync_photos_to_r2.mjs` 隨時可重建。代價是 `/photos` 網頁版顯示不出圖。

## Supabase 降容：逐張查證的結論

519 MB／額度 500 MB。三張大表**兩張不能搬、一張不該搬**：

| 表 | 大小 | 結論 |
|---|---|---|
| `ebook_chunks` | 158 MB | ❌ content 是列表要顯示的 100 字 preview，搬走每次列表都要回 R2；
  且 158 MB 裡有 80 MB 是 24 萬列的固定開銷，搬內容也省不掉 |
| `ai_dialogues`×2 | 71 MB | ❌ `buildDialogueKeywordFilter` 對 prompt/response 做 ILIKE 全表搜尋，
  搬走 `/ai-dialogues` 的搜尋整個失效 |
| `lit_review_sections` | 90 MB | ⚠️ 技術上可搬（唯一無檢索的），但它是**創生哲學改寫的活躍底本**
  （406/520 筆屬 `genesis-philosophy`）且**正被 quality_reviewer 寫入**，已停手 |

🚨 **VACUUM 這條路是死的**：一般 VACUUM 527→528 MB，`VACUUM FULL` 三張表大小完全不變。
先前「死列 11–14%、可回收 15–20 MB」的估計是看錯了統計——那些死列 autovacuum 早已回收。

## 各站台的解析陷阱

**tcnn.org.tw（教會公報新聞網）** `scripts/pct_tcnn.py`
WordPress 且 REST API 未關，逐篇連全文一起給，最省事。以「年」為窗口分頁避開深分頁問題。全文按年打包 JSONL 放 R2；**逐年篇目清單約 6 MB 不進 repo**，由 API 直接讀 R2 供應，否則每次重跑都整批改寫。

**gospel.pct.org.tw（新使者）** `scripts/pct_newmessenger.py --mag`
同一套舊 ASP.NET 版面用 strTID 掛多刊：1 新使者、4 女宣、6 事工說明書。三個坑：
- 內文固定在 `<td id="Zoom">`（其他選擇器都會抓到分享表單）
- **欄目那格的 class 導覽列也在用**，取第一個會拿到空的導覽格，要從題名往上找
- **期名只在文章頁的麵包屑上**，目次頁反而沒有

**www.pct.org.tw（總會重要文獻）** `scripts/pct_documents.py`
🚨 **DocID 是補零三位**（`DocID=001`）；不補零的 `1` 一律回 500，1971–2005 的早期文獻會整批漏掉——三大聲明就在那個範圍。年份取自頁面 `<title>` 的「1971重要文獻」段，比從內文猜可靠。信仰告白不走 DocID，另在 `ab_faith.aspx`（台語漢字／華語／英語三版並列），用 `--static` 收。

**ct.org.tw（基督教論壇報）** `scripts/ct_forum.py`
自寫 PHP，四個坑：
- **文章 ID 不能遞增窮舉**——無效 ID 一律回同一張預設頁（不是 404），只能從分類列表逐頁取
- **日期與作者只在列表頁上**，單篇頁沒有；列表階段就要記下來，否則語料層排不出年表。
  列表頁有**兩種版型**都要收：`div.card`（分類本體，一頁 12 篇，日期在 `span.date`、作者在 `span.author`）
  與 `div.caption`（頁首輪播 6 篇，**每一頁都一樣**，三項擠在 `div.description`）。12+6=18＝`PER_PAGE`。
  兩種的作者都是「刊名連結後面那段純文字」（本報訊／記者梁敬彥／編譯余友梅…），要先把 `<a>` 拆掉再取
- 🚨 **`harvest()` 用 `setdefault` 合併，既有的列永遠不會被更新**。所以「改了 parser 再重跑」
  對已收的兩萬多列一點作用都沒有——補欄位得改成「只填空欄」的合併，
  而且**絕不能蓋掉 `chars`**（那是 `--process` 算出來的，列表頁沒有）
- 署名同一人有多種寫法（`記者梁敬彥`／`梁敬彥`／`記者 梁敬彥`），332 種遠少於 332 個人。
  這是報社署名本身不統一，**照原樣存不要自動正規化**，要合併得人工定奪
- 🚨 **翻頁的終止條件只能看「這一頁還是不是真的列表頁」**（ID 集合與第一頁相同＝已翻過頭）。用「有沒有新文章」判斷，續跑時前幾頁本來就都收過了，會一開頁就誤判到底（社論言論一類因此只抓到 36 篇，修好後 524 篇）
- 🚨 **harvest 每跑完一個分類就覆寫 HARVEST 檔**，中途 kill 掉會把先前較完整的清單覆蓋成半成品（踩過一次：完整的 23,713 篇被覆蓋成只跑到第 10 類的 10,368 篇）。要中斷就等它跑完一輪，或先備份 `C:/tmp/ct_forum.json`
- 🚨 **`--process` 一輪跑完就結束、不會自動接續**——每輪只處理當時清單裡尚未入庫的部分，看到「新增 N 篇」的結尾就是一輪結束，要再下一次
- 🚨 **中斷 `--process` 會留下缺 `chars` 的篇目**。那批全文其實已經上了 R2，只是還沒寫回 harvest；
  重跑 `--process` 只會因為「R2 已有」把它們跳過，字數就一路少算（踩過一次，400 篇少算約 65 萬字）。
  用 `--backfill-chars` 從 R2 讀回長度補上，再 `--publish`
- 🚨 **專題頁判別不能只看子連結數**——每篇文章頁側欄本來就固定掛 8–9 條「相關文章」，門檻設 6 會誤殺 8,960 篇正常報導。專題頁的特徵是子連結明顯多於側欄常數且正文極短（現用 ≥14 且 <1200 字）

**白話字教會公報（1885–1969）** `scripts/pct_poj.py`
補的是 tcnn（2010-12 起）之前那八十四年——長老教會機關報從《台南府城教會報》到
《台灣教會公報》，全以白話字書寫，1969 年才改出華文。論文第二章與第四章前段
（黃彰輝那一代之前）的公共發言只能從這裡看。
來源是師大台文所「台灣白話字文獻館」的 GitHub 鏡像 `Taiwanese-Corpus/Khin-hoan_2010_pojbh`
的 `pojbh.json`（23.5 MB，2,805 筆，其中 2,735 筆有全文）。每篇有**漢羅**與**台羅**兩種寫法。
- 🚨 **這是選輯不是全份**。中研院語言學研究所另有 1885–1969 逐頁掃描並輸入的完整閩語
  資料庫（數位典藏 `LAMINTX0008`，`ascdc.digitalarchives.tw/collection_3334386.html`），
  但那一頁只是藏品層級目錄（型式:合集、數量:1），**沒有任何下載**，管理權在語言所，
  要去信 `ilsecretariat@sinica.edu.tw` 談授權。不要把本批說成「教會公報全收了」
- 🚨 **只收文字不收圖片**。README 載明文字採 CC 授權，圖片另向台灣教會公報社、
  長老教會教會歷史委員會、真理大學牛津學堂、淡江中學校史館授權——不是我們的
- 🚨 **這批是白話字／漢羅台語，不是華語**。策展詞表那些華語詞（本色化／實況化…）
  對 `Kàu-hoē`、`kui-chèng` 一個都比不到，要進語料層得另立台語詞形，
  道理同日文那邊（「無教会」與「無教會」各自成詞，合併會互相污染）
- 按**十年**打包成 JSONL 上 R2，不逐篇一個小檔——論壇報那兩萬多個小檔讓語料層
  掃描慢了一個數量級，這是那次的教訓
- `pianho`（編號）2,805 筆全唯一，可直接當鍵；篇名一律 `漢字 [ Pe̍h-oē-jī ]`；
  日期只到「年/月」且有 17 筆寫「不詳」，**歸自己一桶不做內插**

**laijohn.com（賴永祥長老史料庫）** `scripts/laijohn_biographies.py`
靜態 Big5、無反爬，但站方是義務維護的個人史料庫，延遲放 0.5 秒。路徑 `/archives/pc/<姓>/<姓,名>/…` 本身帶人名代碼，不必解析頁面即可歸戶。**人名代碼要用站方的羅馬字拼法**（黃彰輝是 `Ng,ChiongHui` 不是 `Ng,Chhui`），猜錯會以為沒收到；用篇名反查較保險。宋泉盛不在本土信徒區（長年在海外）。

**無教會** `scripts/mukyokai_sources.py`（單件收錄）、`scripts/nonchurch_magazine.py`（《無境界者》整本）
以「運動」而非機構為單位。日文論著保留原題（`titleOriginal`）另附中譯，`--lang ja`。支援 .docx（宣言、創刊宗旨這類原生 Word 檔，走 zipfile 讀 document.xml，不必 OCR）。
🚨 廖本恩那兩篇論文在 `G:\公事\無境界者雜誌資訊\無教會者雙月刊\`，**不在** `資料\無境界者\`（那裡只有雜誌文章 docx）。

## 國家檔案（檔案管理局）`scripts/archives_gov.py`

戒嚴時期國家如何處置宗教，證據不在報紙而在情治單位的公文裡。26 組查詢、**5,057 筆書目**，
其中 **528 筆可線上閱覽（可直接下載影像）**，其餘須向檔案局申請。

分區：長老教會 2,714（含三神與四人系譜與三個專案代號）／佛教 1,861／一貫道 417／衛理公會 65。
成果同時放 Drive `研究資料/國家檔案調閱/`（總表 txt ＋各區 json）——repo 那份是網站讀的，
Drive 那份才是使用者拿得到的。

**發現的重點**：黃彰輝 91、宋泉盛 28、**王憲治 83（調查局 73）**、黃伯和 49 —— 四人系譜全被監控；
高俊明 582（線上 157，含美麗島偵訊筆錄 388 頁、財產查封 295 頁）；南神 134 遠多於北神 43；
傳道法師 136；印順 4（含「可疑分子考管-印順和尚」與《佛法概論》查禁公文）。

- 🚨 **一律走 curl**。站方 WAF 認**客戶端指紋**不認 IP：Playwright 回一頁
  「The URL you requested has been blocked」而 HTTP 仍是 200（只看狀態碼會誤判成成功）；
  Python requests 連 TLS 握手都被拒；curl 同一時間正常。曾誤判成「IP 封鎖」而停跑一整天
- 🚨 **表單 35 個欄位要連同預設值一起送**。只挑幾個送，關鍵詞會被忽略而回傳整個資料庫
  （三個不同詞都回 4,847,628 筆）。用不相干的詞（鰻魚養殖 11 筆）當對照組驗證
- 🚨 **零結果與請求失敗必須分開回報**。查無資料回的是 187 KB 的表單頁、沒有「共為 N 筆」；
  有結果是 800 KB 以上。只回一個「?」會讓「查過確實沒有」與「抓壞了」混在一起
- 🚨 **每個欄位各用自己的樣式，不要共用一條「非貪婪＋前瞻」通式**。那種寫法兩頭都會錯：
  後面緊接標記時 `.{0,60}?` 會匹配成**空字串**（實測 56.8% 的檔號變空白，而檔號是申請調閱
  唯一必填的欄位），沒緊接時又會吃進隔壁的「檔案起訖日期」（1,636 筆被污染）
- 🚨 **「檢視全部案件」不是一筆檔案**，是同一筆的下層連結，不排除掉會灌水
- 🚨 **「已數位化」不等於「可以下載」**。影像檢視頁的按鈕是「下載全部影像／加入申請書」兩種：
  標「可線上閱覽」者真的能下載，標「須提出申請」者只能加入申請書
- **政治檔案篩選（QueryRange2）送出去沒有作用**，限與不限筆數完全相同，故不跑兩輪
- 檢索詞要用**當時公文的用語**：「印順和尚」有 4 筆而「釋印順」0 筆；
  昭慧法師則是「昭慧」17 筆全為同名者（曾／江／楊昭慧），「昭慧法師」「盧瓊昭」「弘誓」皆 0 筆
  ——這是查過而確實沒有，與沒查過是兩回事，要記下來

## 到館前要用的三份清單 `/research-data/pct/biblio`

三個分頁：華藝期刊書目（1,728 筆）、臺灣博碩士論文、臺灣記憶館藏（444 件，434 件附目次共 12,821 條）。
都只收書目，不取全文。

**華藝** `scripts/biblio_search.py`
搜尋是把一整包 JSON 塞進網址 queryString，結果頁 JS 渲染，故用 playwright。
結果頁有 `#Result_每頁顯示` 可切 50 筆，翻頁鍵在 `.page a`。上限 8 頁＝400 筆／組，
**撞到上限的組要標 `truncated`** 並顯示在頁面上——不標的話截斷會看起來像「這題目就這麼多」。

**臺灣博碩士論文** `scripts/thesis_ndltd.py`
🚨 **一組一組跑、每組存檔**。原本一次把 14 組丟給 node，跑完才回傳——撞到一小時逾時，
一整輪結果全丟。現在 `--search` 會跳過檔裡已有的組，可以直接重下續跑（`--force` 才重跑全部）。
🚨 **驗證碼是「流量觸發」的，不是常態掛著**——兩種先前的判定都只對了一半：
既不是「全站掛驗證碼、不可抓取」，也不是「首頁與檢索都沒有驗證碼」。
站方依**連線 IP** 計量，抓密了就跳出 `驗證碼檢查機制` 那一頁
（「由於目前系統使用人數眾多……敬請輸入畫面中的圖形驗證碼」），停一段時間才退掉。
2026-08-30 實測：`--search` 連跑 13 組全部撞上，14 組只有第 1 組是在冷卻前跑完的。
**不要繞過它**——那是站方為維持服務品質設的；只能放慢、隔開時段、分次跑。
腳本現在會先認出驗證碼並回報 `CAPTCHA：…`，不再只留一句 `#ALLFIELD` 選擇器逾時
（那會讓人誤以為是站方改版）。
🚨 **抓取失敗不寫入結果檔**。原本失敗也照樣存成 `count: 0`，在頁面上會長成
「這個題目沒有論文」，跟真的 0 筆分不出來——踩過一次，13 組全成空章節。
🚨 **截斷與否看站方自報的「檢索結果共 N 筆」，不要看翻了幾頁**。分頁鍵有時會在
同一頁上打轉，翻滿 8 頁、去重後只剩 8 筆，用 `pages > 8` 判就會標成截斷卻說不出
站上到底有多少。現在 JS 一併回傳 `total`，`truncated = total > 取回筆數`。
而且**博碩士論文那一欄原本根本沒顯示截斷標記**（華藝欄有、這欄漏了），
於是「8 筆」看起來就像「這題目就只有 8 篇論文」——正是本節開頭要防的那件事。

其餘三個坑：
- **預設只查「論文名稱」且模式「精準」**，多數複合詞回 0 筆 → 必須勾 `#ALLFIELD_不限欄位`
- **查詢模式不可選「模糊」**（`es1`）：它把詞拆成單字比對，「長老教會 公共神學」會撈回一整頁
  植物生理學論文。用預設的「精準」，詞間空白＝AND
- **「下一頁」是 `input[type=image][name=gonext]`**，沒有 value，用 `input[value="下一頁"]` 永遠抓不到，
  會靜默只取回第一頁；而且它要等結果頁渲染完才掛上，不等就誤判成「只有一頁」
記錄連結帶 session 代碼 `ccd=XXXX` 會過期，故只存書目欄位不存連結。

**臺灣記憶** `scripts/tm_presbyterian.py --list`（書目）／`--toc`（目次，需 playwright）
444 件全部 `DO_authorized=0`「不開放授權」，**只取書目與目次，不取數位影像**。
目次很有價值：帶原書頁碼（例「第十六回：一九○三年十月九日 頁149」），歷史檔案館庫房閉架、
每人每次限 10 件，到館前就靠目次挑件。
單件頁沒有「目次」標題也沒有專屬容器，目次夾在「`<頁次>/<總頁數>`」與「數位物件瀏覽」兩個標記之間，
只能用標記切；切不到就是這件沒有目次。已查過的記 `tocChecked`，沒目次的不必重查。

## 進行中（2026-09-04 交接點）

### 已完成

| 材料 | 數量 | 位置 |
|---|---|---|
| 教會公報新聞網 | 38,451 篇 / 4,730 萬字（2010-12–） | R2 gzip |
| 白話字教會公報 | 2,735 篇 / 344 萬字（1885–1969） | R2 gzip |
| 基督教論壇報 | 23,231 篇 / 4,658 萬字，**署名 100%** | R2 |
| 新使者 | 3,848 篇；總會文獻 121 件；賴永祥 4,221 篇 | R2 |
| 無教會＋無境界者 | 12 件＋159 篇 | R2 |
| **檔案局書目** | **29 組 / 5,057 筆**（可線上閱覽 528） | 站上＋Drive |
| **檔案局影像** | **213 件 / 15,269 張** | Drive 3.6 GB |
| **國史館** | 2 份 / 12.5 萬字（勿外傳） | R2 private＋需登入端點 |
| **華藝書目** | 17 組 / 1,822 筆 | 站上 |
| 王博賢博論 | 236 頁全文（政大 2017，指導蔡源林） | Drive |

### 跑著的（背景，會自己續）

| 工作 | 進度 | 接續指令 |
|---|---|---|
| 國度復興報 | **4,106 / 13,098 篇** | `press_krt.py --process` 反覆 |
| 檔案局影像（長老教會） | 跑中 | `archives_images.py --section pct` |
| 博碩士論文 | **12 組 / 859 筆** | `thesis_ndltd.py --search` 每小時 |

🚨 **重啟前先確認同名的還在不在跑**。press_krt 曾同時 10 份互相覆寫狀態檔、
對 Wayback 的請求量 10 倍——先前被限流有一半是自己造成的。

### 沒做完／待決

- **鍾雲鶯〈敵偽、附匪與邪教？〉正式版**《民俗曲藝》231 (2026.3): 61–107。
  華藝要登入、基金會官網無 PDF、HyRead 403。使用者手上只有初稿（作者標「請勿引用」，
  **不轉錄不上架**）。需從玄奘華藝或北市圖 HyRead 下載
- **語料層待重建**：已接論壇報與國度復興報（後者年份留空），等 KRT 收完跑 `--build`
- **檔案局「政治檔案」篩選無效**，限與不限筆數相同，尚未找到正確參數
- 一貫道剩 3 件影像（梁永順案 99 頁、非法組織活動處理案 820 頁、留學生刊物 823 頁）；
  宋楚瑜省政紀實 8 件已依指示刪除

### 查過而確實沒有（這與「沒查過」是兩回事，寫論文史料說明時可用）

- **釋昭慧**：「昭慧」17 筆全為同名者（曾／江／楊昭慧）；「昭慧法師」「盧瓊昭」（俗名）
  「弘誓」皆 0 筆。唯一待查是調查局「七十八年二月份教育情報存參卷案」
- **釋傳道**：「傳道法師」「中華佛寺協會」0 筆；「菩提長青」35 筆全是九二一埔里
  「菩提長青村」組合屋社福案；「妙心寺」5 筆全不可線上閱覽
- 兩位都是解嚴前後才活躍，不在戒嚴監控檔案的時間範圍——這個空缺本身說得通

### 最重要的發現：四人系譜全被監控

黃彰輝 91、宋泉盛 28、**王憲治 83（調查局 73）**、黃伯和 49。
王憲治是 skill 裡標記「研究最薄弱、最有補白價值」的一位，現在有 83 筆一手檔案。
高俊明 582（線上 157，含美麗島偵訊筆錄 388 頁）；南神 134 遠多於北神 43。
專案代號比人名更能看出國家部署：清寧專案、一二一O專案（美麗島）、二二二專案（南神）。

## 語料層的一條界線

國度復興報進語料層時**年份一律留空**。那批只有 Wayback 快照日、沒有發布日期
（三種欄位都驗過是站台固定值或快照日）。快照日是「發布日的上限」不是發布日，
拿它當年份會讓年代曲線**看起來成立而其實是假的**。

## Wayback 限流的樣子

🚨 **表現是「連線被拒」不是「回應變慢」**：`curl: (7) Failed to connect ... port 443`。
失敗只花 3 秒，所以單獨測一次多半正常，但迴圈裡大部分被拒、重試、再被拒，
五分鐘只成功 3 篇＝每篇 100 秒。**這種情況縮短延遲只會更慢**，方向要相反：
延遲 4s → 15s、連線被拒退避 30/60/90s、連續 12 次熔斷停手。

## 拿不到的

- 《台灣教會公報》1885–2007 掃描檔須向公報社去信索取；**2008–2010 之間沒有免費線上來源**（公報社 2008–2025 PDF 合刊在 Pubu 付費）
- 《使者》（1963–1990，新使者前身）線上無全文典藏
- 女宣雜誌：依語料層的判準（會不會跑語料級運算）評估後**決定不收**
- 校園書房站上只有書目與商品頁、無全文

## 記憶庫併入：project_pct_research_data

2026-08-27 新增 `/research-data/pct`，是論文資料整理的第三張卡（前兩張：台灣衛理公會、印順學派與弘誓）。命名沿用**以來源機構為單位**的原則，不按領域切——領域切會讓同一批材料散在多處。

已收：
- **台灣教會公報新聞網 TCNN** 38,451 篇 / 約 4,700 萬字（2010-12 至今）。tcnn.org.tw 是 WordPress 且 REST API 未關，`/wp-json/wp/v2/posts` 逐篇連全文一起給；以「年」為窗口分頁避開深分頁問題。全文按年打包 JSONL 放 R2，**逐年篇目清單約 6 MB 不進 repo**，由 API 直接讀 R2 供應。
- **新使者** 176 期 3,911 篇。gospel.pct.org.tw 同一套 ASP.NET 版面用 strTID 掛多刊：1 新使者、4 女宣雜誌、6 事工說明書，`scripts/pct_newmessenger.py --mag` 切換。

拿不到的：
- 《台灣教會公報》1885–2007 掃描檔要向公報社去信索取；**2008–2010 之間沒有免費線上來源**（公報社 2008–2025 PDF 合刊在 Pubu 付費）。
- 《使者》（1963–1990，新使者前身）線上無全文典藏。

**Why**：這批是博論第四章「台灣長老教會的公共神學發展」與第六章議題結盟的史料底本，見 [[project_hcu_phd_proposal]]。

**How to apply**：站台解析的三個坑——內文固定在 `<td id="Zoom">`；欄目那格的 class 導覽列也在用，要從題名往上找；期名只在文章頁麵包屑、目次頁沒有。

## 索引補記

- TCNN 教會公報新聞網 38,451 篇(WordPress REST API)＋新使者 3,911 篇(strTID 多刊)

## 記憶庫併入：project_mukyokai_collection

2026-08-28 建的 `/research-data/mukyokai`。**名稱就叫「無教會主義」，不加「日本」**（使用者指定）。

以「**運動**」而非機構為收錄單位——無教會依其主張本來就沒有教會組織，沒有機構可掛，這與其他 collection 以機構為單位不同。收的是**關於**無教會的研究文獻；內村鑑三、矢內原忠雄本人的著作屬原典，仍在 `/collected-works` 的無教會 hub。

**中日雙語**：日文論著保留原題（`titleOriginal`）另附中譯（`title`），`lang` 標語言；頁面上並列顯示。加件用 `scripts/mukyokai_sources.py --add PDF --title-original … --title … --lang ja`。

已收 9 件。中文：陳志忠《日治時期台灣教會經驗初探》（東南亞神學研究院 2005）、張劭瑋《教會外的信徒》（輔大 2019）、廖本恩《無境界的信徒們——與日本有別的台灣無教會史》（CGST 2023，**檔案是日文本**）與〈國家的理想〉（2024）。日文：赤江達也〈帝国日本の植民地における無教会キリスト教の展開〉（立命館 2014，作者時任高雄第一科大）與〈無教会派知識人と近代日本〉（東大 2001）、呉敬姫〈内村鑑三と金教臣〉（名古屋大 2010，**接第二章朝鮮線**）、渡部惇〈無教会キリスト教とは何か〉（明治學院 2016）、佐藤明〈内村鑑三とベルクソン〉（法政 2022）。

🚨 **廖本恩那兩篇在 `G:\我的雲端硬碟\公事\無境界者雜誌資訊\無教會者雙月刊\`**，不在 `資料\無境界者\`（那裡只有他的雜誌文章 docx）。同資料夾另有〈二十一世紀台灣無教會運動宣言〉與《無教會者》創刊宗旨待收。

### 日韓文研究書目與全文（2026-09-11）

**韓文那一側原本完全空白**——但無教會在韓國是獨立發展的一支（金教臣 김교신、咸錫憲 함석헌、宋斗用、盧平久，《聖書朝鮮》성서조선），不補等於少掉半個運動。

- 書目：`data/mukyokai/jp-kr-bibliography.jsonl` **197 筆**（日 96／韓 101；論文 125、學位論文 35、專書 37；1977–2026）。`access` 分四級：open 74／abstract 31／paywall 27／print 65。主題集中在金教臣（68）、咸錫憲（38）、朝鮮（28）、《聖書朝鮮》（17）。
- 全文：`node scripts/mukyokai_fetch_jpkr.mjs --run` 把 open 那 74 篇抓下來（日 40／韓 34，85 MB），落 Drive `研究資料\無教會主義\_jp-kr\`，帳本 `scripts/state/mukyokai_jpkr_ledger.jsonl`。**70/74 有文字層**（1,684 頁／196 萬字），只有朴賢淑四篇（關西學院掃描本，53 頁）要 OCR。

**為什麼那支非用真 Chrome 不可**，curl 一個都過不了：

| 站 | 擋法 |
|---|---|
| JAIRO Cloud（WEKO3）系機構典藏 | curl 一律 **HTTP 406**，換 UA 加 Accept 都沒用 |
| KCI（韓國，34 筆） | 下載是 JS 觸發 `fncDown('KCI_FI…')`，**沒有靜態 PDF 連結** |
| 京大 KURENAI | DSpace 7（Angular SPA），檔案只在 `/server/api/core/…/bitstreams` 底下 |
| J-STAGE（13 筆） | 唯一的 `_pdf` 直連 |

🚨 **CiNii 的「本文」連結回的是內含 PDF 的 ZIP**（`PK\x03\x04`），而 Content-Type 照樣寫 `application/pdf`。不解開就整批判成「不是 PDF」而漏收。
🚨 **逾時與 ECONNRESET 要重試，不可當成「沒有全文」**——第一輪 13 筆敗在這裡（J-STAGE 大檔 90 秒拉不完），加三次重試與 180 秒逾時後全部拿到。
🚨 **RISS 的漢字用 CJK 相容表意文字**（盧＝U+F933、歷＝U+F98C），標題比對看起來一模一樣卻永遠 miss，比對前必須 `NFKC` 正規化。
🚨 **KCI 的查詢參數是 `poSearchBean.queryText`**；猜錯參數名它會**靜默回傳預設清單**，完全看不出查詢沒生效。
查不動的：RISS 學位論文原文要登入（27 筆標 paywall，不是沒有）、DBpia DNS 不通、NDL 那幾本博論多屬個人送信／館內限定，未確認一律不標 open。

### 既有中譯本的可得性（2026-09-11）

REFERENCE-first 要能執行，得先知道書拿不拿得到。實查見 `data/zlib-wanted/mukyokai-zh-found.jsonl`：

- **可買電子書**：內村《基督徒的安慰》（江金龍譯，橄欖 2019，NT$90）、《求安錄》（橄欖）、《寬門！窄路！：論當代傳道精神》（＝『伝道之精神』）、《代表的日本人》（陳心慧譯，遠足）、矢內原《帝國主義下的臺灣》2022 新譯版（大家出版）
- **紙本售完**：矢內原《耶穌傳》（涂南山譯，永望，ISBN 9789865996284）——譯者是在**火燒島監禁期間**譯的
- 🚨 **矢內原的聖經講義在台灣是以叢書名「平信徒聖經注釋」出版的**，拿《詩篇講義》這種原書名去查會查不到
- 🚨 **z-library 的 DiamWall 今天全天不放行**（HTTP 513，真 Chrome 等滿兩分鐘），所以那批中譯本這一輪拿不到；依使用者裁定：**z-lib 拿得到就用譯者的譯名，自譯就照我們的規則**（[[feedback_translation_register_and_titles]]）

相關 [[project_corpus_layer]]、[[project_uchimura_yanaihara]]、[[project_hcu_phd_proposal]]。

## 索引補記

- 中日雙語（日文原題＋中譯）

## 記憶庫併入：project_evangelical_collection

2026-08-28 建的 `/research-data/evangelical`。**收它的理由是對照**：福音派長期主張政教分離、不介入公共，卻在性別議題上高度動員。沒有這條線，博論第六章只會看到「宗教界的結盟」，看不到基督教內部的分裂。

**《基督教論壇報》ct.org.tw**（1965 創刊）是主要來源，與《台灣教會公報》同文類同年代，語料層可逐年並排。`scripts/ct_forum.py`。站方是自寫 PHP，三個坑：

1. **文章 ID 不能遞增窮舉**——無效 ID 一律回同一張預設頁（不是 404），只能從分類列表逐頁取。
2. **日期與作者只在列表頁上**，單篇頁沒有；列表階段就要一併記下來，否則語料層排不出年表。
3. **同一網址形式混著單篇與「專題」頁**（只有導言＋子篇連結），專題頁當成一篇收會污染語料，靠子連結數擋。
4. 🚨 翻頁的終止條件**只能看「這一頁還是不是真的列表頁」**（與第一頁 ID 相同＝已翻過頭），不能看「有沒有新文章」——續跑時前幾頁本來就都收過了，會一開頁就誤判到底（社論言論一類因此只抓到 36 篇，修好後 524 篇）。

規模：21 個分類，列表約 1.7 萬篇以上仍在增加。

其他評估過的來源：華神期刊（站台連不上，可走個別 PDF）、中原宗研所學位論文（福音派系統，本身就是一手材料）、護家盟／下一代幸福聯盟 2013–2018 聲明（多已下架，需 Wayback）。**校園書房站上只有書目與商品頁、無全文，不收。**

相關 [[project_corpus_layer]]、[[project_pct_research_data]]、[[project_hcu_phd_proposal]]。

## 索引補記

- 長老教會的對照面

## 記憶庫併入：project_chengzhong_bulletins

台北城中教會（城中牧區）歷年主日崇拜週報電子化，2026-06-02 完成首版。上架於
`/research-data/taiwan-methodist/chengzhong-bulletins`（索引頁年度篩選＋全文搜尋＋禮儀顏色標記；`[slug]` 詳情頁）。

**關鍵事實：週報全部是原生數位 Word 檔（.doc/.docx），不是掃描影像 → 全程不用 OCR。** 用 Word COM 直接讀文字＋表格（.docx 走 python-docx、.htm 走 bs4）。1,196→1,331 檔抽取 0 錯誤，去重後 **1,250 份**；coverage：節期 99% / 證道者 98% / 經課 100% / 聖詩 76% / 禮儀顏色 65% / 證道題目 61%（題目最弱，但崇拜程序全文都存著可搜尋）。

**Pipeline（reusable，在 repo `scripts/`）：**
- `cz_extract_raw.py` — 走 `城中週報/周報/<year>/` 樹抽段落＋表格 → `c:/tmp/cz_bulletins/raw.jsonl`（保留供快速 re-parse，免再跑慢的 Word COM）。`.doc` table 用 `Range.Text` 一次讀（cell-by-cell COM 會慢 20×）。
- `cz_parse.py` — regex 結構化（節期/顏色/服事人員表/證道者/題目/經課/聖詩）+ Easter computus 回推復活節朝陽崇拜／受難節禮拜日期 → 寫 `public/content/chengzhong-bulletins/index.json` + `<year>/<slug>.json`。
- `cz_reorg.py` — 實體資料夾整理（dry-run 預設，`--apply` 執行，manifest 可回溯）。

**Drive 實體資料夾已整理**（`pong-archive/stores/城中週報/`，git 不追蹤，是 Drive 備份）：清 86 個垃圾檔、解壓 9 個舊年 zip（原檔留 `_原始壓縮檔/`）、分流成 `周報/<year>` + `講章/` + `講道/` + `錄音/{主日,崇拜}`。動作清單存 `城中週報/_reorg_manifest.json`。

**已串到 pong_sermons**（`scripts/cz_enrich_sermons.py`，fill-if-empty）：龐牧師講道 242 場補上證道題目/服事人員(司會/讀經/獻詩/司琴/司獻/招待→worship_team)/詩歌/經課。發現「龐講道但無逐字稿」629 週中 390 週缺稿，**全是無錄影**（佇列無 YouTube）→ 缺口是錄影缺口非轉錄缺口；報告存 `城中教會講道清單/_龐牧師無逐字稿週次.md`。4 筆週報=龐但佇列標⛔別人需人工確認（可能是被誤標的龐講道）。註：發現部分 pong_sermons.scripture_ref 與週報官方經課不符（如 2024-06-09），週報較可信但未覆蓋。

**已補建完整講道史**（`scripts/cz_sermons_extend.py`）：為 386 場「龐講道但無 DB 列」週次建 metadata-only row（無逐字稿、is_published、has_recording=false；title/occasion/服事人員/詩歌/經課取自週報；preacher 依 era map、church_year 以將臨期回推、sermon_type 主日講道/特殊節日）→ pong_sermons 314→**700 列**。經課**不覆蓋**既有列（舊年週報抽取不如 DB 轉錄可靠），只輸出 121 筆 DB↔週報雙向書卷衝突 → `城中教會講道清單/_經課對照需確認.md` 供人工複核。4 筆週報=龐但佇列⛔別人（有錄影）已建列待確認影片是否真為龐講道。

**尚未做（使用者選「先只做週報」）：** 講章/講道/錄音 還沒上站；證道題目 61% 可日後用 Gemini 補強。與 [[pong_sermon_pipeline]]（城中講道 YouTube 轉錄）、[[project_pong_2020_era]] 相關（2019-10 起邱泰耀接任主任，週報服事表可見）。

## 索引補記

- Drive 資料夾已分流整理
