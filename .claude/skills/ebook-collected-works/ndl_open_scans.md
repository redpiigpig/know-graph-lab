> ⚙️ **引擎政策**：OCR 走 Gemini Vision（主）→ Haiku Vision（**須明確下令**，
> 見 [[feedback_ocr_strategy]]）。NVIDIA 那一層是純文字，不能做 vision。

# NDL デジタルコレクション 取源管線（2026-09-07 試點）

無教會譜系裡戰前那批是公有領域，卻**不在青空文庫、也不在 libgen**——只有國立國會
圖書館的掃描本。這一檔記管線本身；書目與版權判斷見
[mukyokai_collected_works.md](mukyokai_collected_works.md)。

> ⚠️ 這個檔名先前被 `scripts/ndl_build.py` 的 docstring 引用，但**檔案一直不存在**
> （上一輪只寫了引用沒寫檔）。本檔補上。下面的 33 部普查數字來自上一輪的交接紀錄，
> **本輪只逐一驗證了試點那一部**，其餘尚未逐 pid 複驗。

## 🚨 判公開範圍只有一個可靠辦法

    curl -s -o /dev/null -w "%{http_code}" https://dl.ndl.go.jp/api/iiif/{pid}/manifest.json
    # 200 ＝ インターネット公開（可用）
    # 404 ＝ 館內限定／個人送信（不可用）

目錄 metadata **不帶**這個欄位，別想從書誌判斷。
「圖書館‧個人送信」看起來可用，但**居住在日本境外者不能使用**，不要算進來。
不要繞過存取控制。

## 兩個 API 的分工

| 用途 | 端點 | 備註 |
|---|---|---|
| 書誌＋**目次**（分章靠它） | `lab.ndl.go.jp/dl/api/book/{pid}` | `page` 偶爾回 0，那時回頭數 IIIF canvas |
| 影像＋公開範圍探針 | `dl.ndl.go.jp/api/iiif/{pid}/manifest.json` | 也是唯一可靠的公開範圍判準 |

## 普查（上一輪交接數字，未逐 pid 複驗）

| 作者 | 可直接下載 | 備註 |
|---|---|---|
| 畔上賢造 | 16 | 全球 PD（卒 1938） |
| 矢內原忠雄 | 9 | 日本 2012 起、台灣皆 PD |
| 藤井武 | 7 | 全球 PD（卒 1930） |
| 內村鑑三 | 1 | 全球 PD |
| **合計** | **33** | |

黒崎幸吉（28 部）、塚本虎二（35 部）、南原繁（23 部）數位化了但**全是館內限定**
（卒後 70 年未過），不可取用。

## 管線（`scripts/ndl_build.py`，`scripts/tests/test_ndl_build.py` 23 例綠）

    python scripts/ndl_build.py --probe 1099766              # 書誌＋分章
    python scripts/ndl_build.py --fetch 1099766              # 影像 → c:/tmp/ndl_cache/{pid}
    python scripts/ndl_build.py --ocr   1099766              # Gemini Vision，一頁一個 checkpoint
    python scripts/ndl_build.py --build 1099766 --slug ...   # → ndl_data/{slug}/secN.json

輸出與 uchimura／howes **同形**（`heading`／`title_zh`／`src`／`zh`），
才接得上 `uchimura_auto.py` 的 checkpoint／翻譯／上架那一段。

### 本輪加的兩道保險

- **`refuse_empty_build()`**：所有 section 都沒有正文就拒絕寫檔。不擋的話會產出
  一整套結構正確、目錄齊全、每章卻都沒有字的 `secN.json`——**頁面看起來完全正常**。
  這道閘在本輪真的擋下了一次（OCR 因額度沒跑成，接著 build 就被擋）。
- **`_client_for()`**：`genai.Client` 一定要綁在變數上。寫成
  `genai.Client(...).models.generate_content(...)` 那個 client 是暫時物件，
  請求還沒回來就可能被 GC 關掉，報
  `RuntimeError: Cannot send a request, as the client has been closed.`
  ——錯誤訊息完全看不出是這個原因，踩過一次。

## 試點：畔上賢造《無教会主義》1934（pid 1099766）

| 步驟 | 狀態 |
|---|---|
| IIIF manifest | ✅ 200（インターネット公開） |
| 書誌 | ✅ 東方書院 1934，影像 18 頁 |
| 目次分章 | ✅ 9 章全部解析成功（見下） |
| 影像下載 | ✅ 18 張 → `c:/tmp/ndl_cache/1099766` |
| OCR | ✅ Gemini 18 頁／19,091 字（目次 canary 89%） |
| build | ✅ 9 章 53 段 |
| 翻譯 | ✅ Haiku 53/53 段繁中 |
| 上架 | ✅ `e0000000-…-0001`，11 chunks；store hub 已改 `status: done` |

分章結果（影像號範圍）：

    3–3    第一　教派ではない          10–10  第六　聖書的基督教の提唱
    4–4    第二　起源                  11–11  第七　信仰第一主義の提唱
    5–5    第三　現状                  12–14  第八　日本的基督教の提唱
    6–7    第四　教會に對する抗議      15–18  第九　無教會主義の傳道法
    8–9    第五　無教會主義についての誤解

**目次分章這一段完全不用改**——`parse_toc_entry` 把 NDL 那三種寫法都吃下來了，
9 章一次到位。

### ⛔⛔ Haiku Vision 在這批材料上會**編造內容**（2026-09-07 實測，勿再試）

使用者明確授權用 Haiku 之後跑了，結果比「跑不動」更糟：**它會吐出通順但整段虛構的日文。**

拿影像 3 左半頁（標題頁＋第一章開頭，我自己看原圖讀得清清楚楚）當對照：

| | 內容 |
|---|---|
| **原圖真值** | 近頃「無教會主義」といふことが、日本の基督教界の一つの大きな問題となつてきた… |
| Haiku 輸出 | 宗教と「無教會主義」と称することは、日本の基督教發展の二つの大きな問題であつた… |

作者名還從「畔上賢造」變成「昭上賢造」「卍上賢造」「唵上賢造」（每次不同），
目次整頁消失，第一次跑時甚至憑空生出一份**完全不存在的目次**
（第一 無教會主義の本質／第二 信仰的基礎…頁碼到 175，而全書只有 18 張影像）。

**排除過的變因**（不是這些的問題）：

| 變因 | 處置 | 結果 |
|---|---|---|
| 解析度太低 | 1800px → 全解析度 3575px | 字數 9,588→16,072，仍在編 |
| 跨頁沒切開 | 加 `split_spread_boxes()` 切左右頁（各 1787×2787） | 切圖經目視確認正確，仍在編 |
| prompt 太鬆 | 改成嚴格逐字 prompt（禁止推測／讀不出寫 □） | **更差**（359 字，連「教派ではない」都不見了） |

失敗模式是**部分錨定的編造**：它讀到零星字詞（「教派ではない」「一つの大きな問題となつてきた」），
再用通順日文把中間補起來。這比完全讀不出來危險得多 —— 每一頁單獨看都毫無異狀。

**結論：這批直排舊字舊假名不要用 Haiku Vision。** 不是 prompt 問題，也不是解析度問題。

### 🛡️ 因此加的兩道閘

- **`toc_match_ratio()` 目次 canary**：目次頁的內容我們本來就從 NDL API 知道，
  拿它驗 OCR 有沒有在讀圖。對上 <50% 就拒絕產出。
  對這輪的 Haiku 結果實測**對上 0%**，成功擋下。
  這是目前唯一能自動抓到「編造」的辦法 —— 因為只有這一頁有獨立真值。
- **checkpoint 依引擎分目錄**（`ocr-gemini/`／`ocr-haiku/`）：不分的話，
  等 Gemini 額度回來重跑，會把 Haiku 編造的那批當成「已完成」直接跳過。

### ⛔ OCR 卡在哪

`gemini-2.5-flash` 免費層是 **20 requests／day／key** 的**日額度**（當天不恢復，
見 [[reference_gemini_free_tier_quotas]]）。本輪開跑時當日額度已被其他工作用完，
兩把 key 連續 429 → 依 [[feedback_ocr_two_strike_quota]] 自動退出，**0 頁完成**。

### ✅ 後來怎麼解的：**根本不是沒額度**

真正的原因是 `ocr_page()` 把「兩次連續 429 就退」實作成「試兩把 key 就放棄」，
而本機有 **7 把 key**，另外 5 把從沒被試過。我因此誤判成「今天沒額度」，
才繞去試 Haiku Vision（結果它會編造）。

**日額度在台灣時間 15:00（太平洋 00:00）重置**。修好 key 迴圈之後一次跑完 18 頁。

🚨 **教訓**：two-strike 那條規矩的本意是「池子乾了就別再捶」，不是「試兩把就放棄」。
判斷「沒額度」之前先確認**所有** key 都試過了。

### 這一輪踩到並修掉的四個坑

1. **目次被吞進第一章**：NDL 是跨頁掃描，一節的第一張影像常常還印著別的東西
   （目次、書名頁、前一章結尾）。整張當內文的話，整份目次會跑進第一章。
   → `strip_before_heading()`：正文從該節自己的章名之後開始，**取最後一次出現**
   （章名在目次裡也會出現一次，取第一次等於沒切）。
2. **canary 低估好的 OCR**：Gemini 把 9 個章名全讀對了，canary 卻只給 56%——
   因為書上印「敎」「現狀」而 NDL 索引寫「教」「現状」。比對前先正規化異體字後
   是 89%，只剩「起源→源」一個真的漏字。**異體字表只在比對時用，不改寫正文。**
3. **build 會吃掉譯文**：`ndl_data/{slug}/secN.json` 同時是 build 的產出與翻譯的
   checkpoint（`src` 由 build 寫、`zh` 由 uchimura_auto 填），修完 OCR 再 build
   一次會把 `zh` 清空。→ `has_translations()` 擋下，要重做得明確加 `--force`。
4. **跨頁要切開再 OCR**：整張跨頁丟進去等於每頁只剩一半解析度。
   `split_spread_boxes()` 切左右頁，**右半頁先讀**（日文直書右起）。
   順序反了整本文意會倒著接，而且每頁單獨看都正常。

## 🥇 取源首選：NDL 官方 OCR（2026-09-08 起改用，視覺模型退為輔助）

**這一節是本檔最重要的一段。** 先前整條管線建立在「用視覺模型 OCR 掃描影像」上，
其實 **NDL 自己就提供官方 OCR 全文**，而且準確度遠高於視覺模型。
我繞了一大圈（Haiku 編造 → Gemini 逐字錯誤 → 兩次 OCR 互比 271 處分歧）才發現。

    https://lab.ndl.go.jp/dl/api/book/layouttext/{pid}     # 帶版面結構的 XML（ZIP）★首選
    https://lab.ndl.go.jp/dl/api/book/fulltext-json/{pid}  # 平鋪全文＋逐字座標

🚨 **端點格式是 `/book/layouttext/{pid}`，不是 `/book/{pid}/layouttext`。**
我先前猜的 `/book/{pid}/text` 全部回 SPA 殼（HTTP 200 但內容是 HTML），
又一次踩到「200 不代表存在」——跟 BDCC 的 500 是同一種陷阱。

### 準確度對照（影像 4，拿原圖逐字比對）

| 位置 | Gemini Vision | NDL 官方 |
|---|---|---|
| 敎派**別**を無視 | 漏「別」 | ✅ |
| 加**へ**て | 加えて（假名改新式） | ✅ |
| 無敎**會的**精神 | 無敎**的の**精神 | ✅ |
| 保**續** | 保持 | ✅ |
| 以上**に精神**である | 以上は精である | ✅ |
| 呼ば**ね**ばならぬ | 呼べばならぬ | ✅ |
| 人々**の**間に**一つ**の | 人々は間につの | ✅ |

**Gemini 錯的每一處，NDL 都對。** 而且 layouttext 還附帶版面資訊，
段落結構可以還原（見下），不必再靠空行猜。

### layouttext XML 怎麼用

每行一個 `<LINE>`，帶 `ORDER`（全域閱讀順序）、`X`/`Y`（直排欄位座標）、
`TYPE`（本文／タイトル本文／ノンブル／柱）、`CONF`（信心值）。

- **一定要照 `ORDER` 排**，不能照 XML 出現順序 —— TEXTBLOCK 是按版面區塊分的，
  跨區塊時兩者不一致。
- **段落靠首行縮排還原**：同段各行的 Y（欄頂）齊平，新段落第一行低一個字
  （本書約 45px，門檻取 35px）。🚨 只有**第一行**縮排，續行回到欄頂。
- **章名（タイトル本文）要斷段**（前一章結尾不可和新章開頭黏成一段），
  但**頁碼與柱不可斷段** —— 它們只是版面裝飾，夾在行序中間，斷了會把一段切兩半。
- 章名本身不要用版面上那一行：常常是亂序的（實例：「第二　起源」讀成「第二 源 起」）。
  章名用 NDL 目次 API，那個是對的。

### 🚨 NDL 唯一的短處：字集外的舊字體印成「〓」

本書 391 處。**不是固定同一個字**：`無〓會`＝敎、`聖書之〓究`＝硏、
`益〓`＝疊字記號々、`たま〓〓`＝くの字点〳〵、`みなもとが〓くも`＝清。

補法是**兩邊互補**：NDL 給正確字句，視覺模型給它編不出的字形，
`fill_placeholders()` 逐字對齊後填回去。實測 397→50（87%），
再用上下文規則補到剩 3，最後 1 處看圖裁定（`清`，由對句
「みなもと濁りて末の清からう筈がない／みなもとが清くも」確認）。
🚨 對不齊就留著 `〓` —— **寧可留記號也不要填錯字**，填錯的混在正文裡看不出來。

### 這本的精修結果

| | 修前（Gemini） | 修後（NDL 官方） |
|---|---|---|
| 段落 | 53 | 59（版面結構還原，段落更準） |
| 字數 | 15,722 | 20,212 |
| 逐字精度 | 一頁抽驗兩個語意錯 | 與原圖逐字吻合 |

另外確定性修掉的兩類：
- **新字體 128 處**（`restore_old_forms()`）：1934 年的書全是舊字體，
  新字體 1946 年才有，OCR 吐出的一律是錯。
- **`教`→`敎` 273 處**：NDL 對這個字 391 次都輸出 `〓`、只有 2 次 `教`，
  代表原書印的是它字集外的 `敎`。補 `〓` 時從視覺模型拿到的 `教` 要統一回去。

### ⚠️ 舊記錄：視覺模型 OCR 的逐字精度（已被上面的作法取代）

Gemini 讀得出來，但不是逐字無誤。只抽驗影像 3 一頁就抓到**兩個會改變語意的錯**
（已對照原圖修在 checkpoint 上，改 checkpoint 而非產出，重建才不會跑掉）：

| OCR | 原書 | 差別 |
|---|---|---|
| 最も**歡ばしい**ことである | 最も**歎かはしい**ことである | 可喜 ↔ 可歎，**意思相反** |
| 現在の**新興**教會 | 現在の**新教**教會 | 新興的教會 ↔ 新教（Protestant） |

一頁兩個，全書 18 頁 —— **其餘各頁尚未逐頁對照原圖**。這本目前是「可讀」不是
「精修」，日後要精修就逐頁調 IIIF 影像與 `ocr-gemini/*.txt` 對讀。

## 接手清單

1. 額度重置後跑 `--ocr 1099766` → `--build 1099766 --slug muky-shugi`。
2. 校 OCR：直排舊字舊假名是這批的難點，`clean_ocr_text` 只處理了振り仮名與頁碼，
   **舊字體有沒有被 Gemini 偷偷轉成新字體要抽查**（prompt 已明令禁止，但要驗）。
3. 接 `uchimura_auto.py` 翻繁中、上架，並在 store 的畔上賢造 hub 把該部
   `status` 由 `planned` 改為 `done` 並填 `ebookId`。
4. 其餘 32 部逐 pid 複驗 IIIF manifest 後再排隊。
