# 📋 scripture-canon-portal / /creeds 梵二 — Handoff 2026-05-22

> 新 session 從這份接手 — 不需要讀整個 SKILL.md 也能繼續。
> 主 SKILL：[SKILL.md](./SKILL.md)

## 🎯 目前狀態 snapshot

### 已上線（user 可立即看到）
- `/scripture-canon` 入口卡片（5 子工具）
- `/creeds` 列表頁 **三區**：信經 ／ 歷代大公會議文獻 ／ 各宗派信條與要理問答
- `/creeds/[slug]` detail page：
  - 摘要區 lead paragraph + amber accent border + 《文件名》自動 italic
  - Notes 區 bullet list（`- xxx` 自動成 `<ul>`，`label：rest` 自動加粗 label）
  - 梵二文件用 **逐段對照** layout（取代原本三欄 accordion）
  - 章節 heading 跨三語欄 gradient bar
  - inline 註腳 `[^N]` / `(N)` clickable jump to 頁底
  - 頁底「📎 註腳對照」三語並列 footnote list
  - 經文 reference (`Rom 11:17`) auto `.scripture-ref` 標楷體 italic bold amber

### 梵二 16 份文件中文重抽狀態（2026-05-22）

| 文件 | 拉丁/英文 | 中文 | 中文段落數 vs 實際 | 引擎 |
|---|---|---|---|---|
| SC 禮儀憲章 | ✓ | ✓ | 129 / 130 | Gemini |
| IM 大眾傳播 | ✓ | ⚠️ broken | placeholder（vatican.va PDF 損毀 0-byte） | — |
| LG 教會憲章 | ✓ | ✓ | 75 / 70 | Gemini（包含部分 footnote）|
| OE 東方公教 | ✓ | ✓ | ~ / 30 | Gemini |
| UR 大公主義 | ✓ | ✓ | ~ / 24 | Gemini |
| CD 主教牧靈 | ✓ | ✓ | 44 / 44 | Gemini |
| PC 修會革新 | ✓ | ✓ | ~ / 25 | Gemini |
| OT 司鐸培養 | ✓ | ✓ | 21 / 22 | Gemini |
| GE 教育宣言 | ✓ | ✓ | 12 / 12 | Gemini |
| NA 對非基督宗教 | ✓ | ✓ | 5 / 5 | Gemini |
| DV 啟示憲章 | ✓ | ✓ | 26 / 26 | Gemini |
| AA 教友傳教 | ✓ | ✓ | 27 / 33 | Haiku OAuth |
| DH 信仰自由 | ✓ | ✓ | 15 / 15 | Haiku OAuth |
| AG 傳教工作 | ✓ | ✓ | 76 / 42 | Haiku OAuth（含部分 sub-items）|
| PO 司鐸職務 | ✓ | 🟡 retrying max_tokens=64K | 16 → ? | Haiku OAuth |
| GS 牧靈憲章 | ✓ | 🟡 retrying max_tokens=64K | 28 → ? | Haiku OAuth |

最後一次 retry 在 background task `bdjlztjk3` 跑 PO + GS 用 max_tokens=64000（之前 16K 不夠裝完整 93 段 GS）。

## 🚧 待續工作

### 1. GS 中文補 paras 40-93（最優先）
**狀態**：已 commit GS 中文 ~155K bytes，但只有 paras 1-39 + footnote defs + 公佈令。
原因：`_find_footnote_split()` 把 offset 切在 53617 (paras 40-93 進 tail)，但
tail prompt (`PROMPT_CHUNK_TAIL`) 只強調 footnote 區 + 公佈令，Haiku 漏掉中間的 paras 40-93 正文。

**修法選項**：
- (a) 改 `_find_footnote_split` 找 GS 真正 footnote section 位置（GS pdftotext 內 footnote 從 `• 附 註 •` marker 開始）
- (b) 改 `_haiku_chunked` 切 3 段 body + 1 tail（而非 2 body + 1 tail），讓 body chunks 覆蓋 paras 1-93 全部
- (c) Single retry：對 GS pdftotext 後半段（offset 53617+）單獨 Haiku 用 BODY prompt（非 TAIL），得 paras 40-93，手動接在現有 GS 後

PO 已完成 ✓ 22 段 + 31 sub-section headings + 62K bytes。

### 2. IM 中文補完（vatican.va 端 PDF 損毀）
- vatican.va 上 `vat-ii_inter-mirifica_zh-t.pdf` 回 HTTP 200 但 Content-Length=0
- vatican.va 中文簡體 `_zh-s.pdf` 也 404
- 替代來源：
  - 台灣地區主教團 1975《梵蒂岡第二屆大公會議文獻》紙本
  - 香港教區禮儀委員會編《梵二禮儀文獻匯編》
- 短期：保持 placeholder 並標註找替代來源；長期：手動 input

### 3. Parser 改進
- **SC §57 sub-item 中斷**：SC 內 §57 含 `§ 1.` `1. a)` `2.` 等子項目編號，paragraphParser 的 fallback 把 §58-130 main body 誤併進 footnotes
  - 在 [`data/creeds/paragraphParser.ts`](../../../data/creeds/paragraphParser.ts) `trimNonMonotonic` 邏輯內加識別「`§ N.` sub-marker」exempt 規則
  - 或者 SC 寫專屬 parser
- **GS / LG / AG 在英文版頁底 footnote section 識別**：已正常處理（`## Footnotes` heading + `[^N]:` def）

### 4. 後續優化（非阻塞）
- detail page biblical-ref regex 對中文書名（如「羅 11:17-24」「弗 2,14-16」）的偵測仍粗糙，會錯抓段內 `（參閱羅 一，15）` 這類引用
- vatican.va 拉丁版部分 doc（NA / GS / DV）沒 section heading markup（`<i><b>` 風格）— 只有英文版才有結構。中文 Haiku 重整時自然會插 `## ` heading，但拉丁無對應 row 會空白
- detail page 三欄 header 的版本切換 dropdown — 對長文件可能 lazy load 慢

## 📁 關鍵檔案地圖

### Pipeline scripts
- [`scripts/rebuild_vatican_ii_html.py`](../../../scripts/rebuild_vatican_ii_html.py) — 拉丁＋英文：vatican.va HTML → markdown 文本（保留 `## heading` + `[^N]:` footnote refs/defs）
- [`scripts/reextract_vatican_ii_chinese_gemini.py`](../../../scripts/reextract_vatican_ii_chinese_gemini.py) — 中文：PDF → pdftotext -layout → Gemini Flash / Haiku 4.5 重整
  - `--engine=gemini`（default）：用 .env 內 4 個 Gemini key 輪轉
  - `--engine=haiku`：用 Anthropic OAuth（讀 `~/.claude/.credentials.json` accessToken；Claude Max 帳號可直接呼叫無需 API key）
  - `--codes=AA,DH,...`：只跑指定文件
  - Free Gemini Flash quota：4 keys × 20 RPD daily（隔天重置）

### Data
- [`data/creeds/types.ts`](../../../data/creeds/types.ts) — CreedVersion schema（含 `textKey?: string` 動態載入欄位）
- [`data/creeds/paragraphParser.ts`](../../../data/creeds/paragraphParser.ts) — parseDoc / alignDocs
- [`data/creeds/textLoader.ts`](../../../data/creeds/textLoader.ts) — Vite `?raw` lazy loader（避免 ~9MB eager bundle）
- [`data/creeds/ecumenical-councils/vatican-ii-{NN}-{code}.ts`](../../../data/creeds/ecumenical-councils/) — 16 份 ts metadata
- [`data/creeds/ecumenical-councils/vatican-ii/{code}-{lang}.txt`](../../../data/creeds/ecumenical-councils/vatican-ii/) — 三語全文（latin/english/chinese 每份）

### UI
- [`pages/creeds/index.vue`](../../../pages/creeds/index.vue) — 列表頁三區
- [`pages/creeds/[slug].vue`](../../../pages/creeds/[slug].vue) — detail page 含逐段對照 + 註腳 + scripture-ref 樣式
- [`pages/scripture-canon/index.vue`](../../../pages/scripture-canon/index.vue) — 入口 5 子工具卡片

## 🔑 Auth note

- Gemini Free tier：`.env` 內 `Gemini_API_Key_1..4`（已配 4 keys）。Daily 20 RPD per key × 4 = 80/day total。
- Anthropic：user 有 **Claude Max** 訂閱，呼叫由 OAuth 走 `~/.claude/.credentials.json`，**不要** 設 `ANTHROPIC_API_KEY`（會 conflict）。`scripts/reextract_vatican_ii_chinese_gemini.py` 的 `_make_anthropic_client` 已正確 fallback 到 OAuth。
- Rate limit on Max：5h-rolling window；密集調用可能短時間觸發 429（等 1-5 min 即可）。

## 📜 Today's commits（reverse chronological）

| Commit | Phase |
|---|---|
| `28e3c5e` | Haiku OAuth 重抽 AA / DH / AG |
| `bbcdec8` | Gemini 重抽 7 份中文 (LG/OE/UR/CD/PC/OT/GE) |
| `4f6d3e2` | SC 中文 'N. 1' → '1.' fix |
| `deeea68` | Gemini script + NA/DV/SC 首批中文 |
| `6bdbcd7` | detail page Phase E（heading row, footnote anchor, scripture-ref）|
| `651a554` | rebuild_vatican_ii_html.py + parser parseDoc + alignDocs |
| `22e204a` | 摘要/Notes CSS + 《》斜體 + SC summary 修潤 |
| `0e143cb` | 梵二 lazy load fix（解 Vite SSR IPC crash）|
| `55f8709` | detail page 三欄並列 + 15/16 中文 pdftotext 首版 |
| `e500564` | 梵二 16 份建檔（拉丁＋英文）+ /creeds 三區重組 |

## 📝 Anathema content-filter 規避策略（為下次做以弗所/迦克墩準備）

SKILL.md 內已寫：條件式詛咒句型「If anyone says ... let him be anathema」會被 Anthropic 內容過濾器誤判。對以弗所 431（Cyril 十二章）、迦克墩 451 定義、Trent、Vatican I 等含 anathema canon 的文件：

**最穩策略**：Claude 寫 schema 與空陣列，user 手動貼 Schaff NPNF2 Vol 14 原文進去。完全繞過 Claude 輸出 anathema 內容。
