# 祆教中譯交接（2026-09-18）

接手後**先跑這三行**，數字對不上就先讀〈已知的坑〉那一節，不要急著動翻譯。

```bash
python -X utf8 scripts/avesta_zh_gate.py          # 產物閘：譯出來的是不是中文
python -X utf8 scripts/avesta_translate.py --audit # 專名有沒有照詞庫
powershell -NoProfile -Command "Get-CimInstance Win32_Process -Filter \"Name like '%python%'\" | Where-Object { $_.CommandLine -like '*avesta_translate*' } | Select-Object ProcessId,ParentProcessId"
```

---

## 一、現況

| | 段數 | 比例 |
|---|---:|---:|
| 總段落 | 2,939 | — |
| 原文 | 2,913 | 99.1% |
| 英譯 | 2,604 | **88.6%（已達 SBE 上限，見下）** |
| 中譯 | 2,090 | 71.1% |
| **待譯** | **849** | |

翻譯程序在跑：**PID 5872**（`_whisper_venv\Scripts\python.exe`，`HELLENIKA_ENGINE=nvidia`），
log 在 `%TEMP%\avesta_tr7.log` / `.err`。**它會自動補譯被清空的段落**，不必重啟。

### 續跑指令（程序若已結束）

```powershell
$env:HELLENIKA_ENGINE = 'nvidia'
Start-Process -FilePath "C:\Users\user\Desktop\know-graph-lab\_whisper_venv\Scripts\python.exe" `
  -ArgumentList '-X','utf8','scripts/avesta_translate.py','--all' `
  -WorkingDirectory "C:\Users\user\Desktop\know-graph-lab" `
  -RedirectStandardOutput "$env:TEMP\avesta_tr8.log" -RedirectStandardError "$env:TEMP\avesta_tr8.err" `
  -WindowStyle Hidden
```

---

## 二、還沒做完的四件事

### 1. 中譯剩 849 段（主要工作）

程序自己會跑完，但引擎狀況會變。目前引擎鏈的實測狀況：

| 引擎 | 狀況（2026-09-18） |
|---|---|
| Gemini | **429 配額用完**。7 把 key × 免費層 20 次/天，隔天恢復 |
| NVIDIA | **可用**，`deepseek-ai/deepseek-v4-flash-0731` |
| OpenRouter | 429，553 次全滿 |
| Haiku | 已走了 73 批。**使用者授權「Gemini 優先，不行就用 Haiku」** |

### 2. 專名稽核剩 46 段次沒照詞庫

`--audit` 會列出來。已知的大宗：Yasna→亞斯納 6 段、Yima→伊瑪 6 段、
yazata→亞扎塔 4 段。**這些要清空重譯**，但 `--audit` 目前沒有 `--reset`，
得手動挑出 ref 再清。

### 3. 英譯 88.6% 是 SBE 的上限，不是抓漏

實測確認過，**不要再去抓一次**：

- **8 章**是 SBE（Mills）用交叉引用取代正文：`5.(identical with Yasna 37)`、
  `64.(See Y46.3; Y50.6-11)`、`72.(See Y61)`，另有 Y2／Y3／Y12／Y29／Y63
- **52 章**是 SBE 分節比阿維斯陀原文粗——Y22 原文 27 節，Mills 只譯成 6 節
- avesta.org 的 Yasna **只有 SBE 一種英譯**，`yasna_mithra.htm` / `yasna_jamaspa.htm`
  是兩種字型的**轉寫**不是英譯

**可做的改善**：把交叉引用的目標章節譯文代入那 8 章，並在欄位標明
「SBE 此處作 see Y37，本站代入該處譯文」。**不可默默代入。**

### 4. 摩尼教那邊：19 段對齊不上

`sabuhragan-turfan` 80 段中 19 段有原文無英譯。**右欄英文是完整的**，
問題在 `choose_scheme()` 挑了稀疏的 `{y.N}` 節號當對齊鍵。
試過用括號行號當第四種鍵，**已回退**——原因寫在
`scripts/manichaean_iams.py` 的 `MARK_SCHEMES` 上方，再試之前務必先讀。

---

## 三、已知的坑（每一個都實際踩過）

### 🚨 EXTRA 不可覆蓋詞庫

`avesta_translate.py` 的專名表從詞庫拉，但曾寫成 `table.update(EXTRA)`，
而 `EXTRA` 硬編了 `"Sraosha": "斯勞沙"`，把詞庫已定的**斯魯沙**靜默蓋掉，
污染全站 109 處。**已改成 `setdefault` 並印出衝突清單**——
看到那個警告就去刪 `EXTRA` 裡的那一條，不要反過來改詞庫。

### 🚨 「拉丁嵌字」要往上追專名表，不是清掉重譯

Yt 12 有 25 段「在 Karshvare Arezahi」這種半英半中，症狀是嵌字，
**根因是七大洲不在 `PLACES_WANTED` 白名單裡**。不修白名單，重譯還是一樣。
（七洲定名已補，依元文琪 Yt 10.15。）

### 🚨 稽核只掃 `segments` 會漏掉一半

修 Sraosha 時我的檢查回報「已清乾淨」，grep 卻還抓得到——
**大半在各檔頂層的 `names` 快照欄**，不在 `segments` 裡。
改檔前先確認迴圈掃到哪些欄位。

### 🚨 venv 的 python 會 re-exec 出子程序，別當成兩個實例

`_whisper_venv\Scripts\python.exe` 是 shim，`Get-Process` 會看到父子兩個 PID。
我曾誤判成重複啟動而停掉，**結果把正在跑的也停了**。
看 `ParentProcessId` 判斷；真的有兩個實例時，它們會互相覆蓋 JSON。

### 🚨 驗「有沒有在做事」不能看 CPU

等 API 回應時 CPU 本來就不動（實測 45 秒 +0.0s，記憶體 113MB 正常）。
**要看譯出段數的增量**，不是 CPU 也不是 PID 活著。

### 🚨 引擎回 429/503/504 先驗模型名

整夜 0 段落地，錯誤看起來像配額或服務中斷，實測是模型名：
同一把 key、同一個 endpoint，`nvidia/nemotron-3-super-120b-a12b` 長期 503，
`deepseek-ai/deepseek-v4-flash-0731` 回 200。已改回。

### 🚨 推理模型的 `<think>` 會讓整批被丟掉

`parse_json_reply()` 負責剝：``` 圍欄 → `<think>…</think>` → 未閉合的 `<think>`
→ 取最外層 `{…}`。四個 doctest 涵蓋這四種長相。

---

## 四、定名規矩（動任何譯名前必讀）

1. **使用者定名是最高原則**，不進計分也不受出處閘約束。
   查到新證據只在 `recommendation_reason` 記一筆備查，**不可拿去翻案**。
2. 祆教譯名以**元文琪《阿維斯塔》**為權威（全文在電子圖書館，274,078 字，
   可直接統計出現次數當證據）。
3. 詞庫 `name_recommended` 是絕對權威；`EXTRA` 只補詞庫沒有的。
4. 🚨 從元文琪那本取任何專名前，**先驗簡轉繁的「里／裡」污染**——
   opencc 會把「法里东」轉成「法**裡**東」。音譯用「里」不用「裡」。

全文見 `.claude/skills/translation-glossary/naming_rules.md`。

---

## 五、相關工具

| 腳本 | 用途 |
|---|---|
| `avesta_translate.py --all` | 逐段翻譯 |
| `avesta_translate.py --refresh-names` | 從詞庫重拉專名表（改完詞庫要跑） |
| `avesta_translate.py --audit` | 專名有沒有照詞庫 |
| `avesta_zh_gate.py [--reset]` | 產物閘；`--reset` 只清驗不過的那幾段 |
| `avesta_yuan_reference.py` | 元文琪譯本對照欄 |
| `manichaean_iams.py --build <slug>` | 摩尼教 IAMS 取源 |

🚨 `--reset` **只清驗不過的段落，不整檔重來**：萬迪達德最怕同一個公式
在不同批次譯得不一樣，好譯文重譯反而製造分歧。
