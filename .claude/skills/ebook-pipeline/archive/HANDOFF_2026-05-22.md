
> ⚙️ **引擎政策（2026-06-04 更新）**：所有 LLM 工作一律**優先用 NVIDIA（輝達，`https://integrate.api.nvidia.com/v1`，預設文字模型 `deepseek-ai/deepseek-v4-flash`，4 把 key 輪流＋間隔節流避免 429）**，第二層 fallback 用 Gemini，**第三層救急才用 Haiku（NVIDIA→Gemini→Haiku；前兩個免費池都用罄時才動 Haiku）**。視覺類用 NVIDIA 視覺模型（如 `nvidia/llama-3.1-nemotron-nano-vl-8b-v1`）。

# Handoff 2026-05-22 — 新 session 接續

上一 session 的進度全紀錄 + 目前正在跑的 background tasks + 下一個 session 要做的事。

## 🔄 目前 background tasks

**沒有跑中** — 上次 Haiku run (bkqtwfyvo) 已完成 exit。新 session **要主動啟動** Haiku worker：

```bash
"C:\Users\user\AppData\Local\Python\bin\python.exe" -u scripts/ocr_with_gemini.py run --engine haiku 2>&1 | tee scripts/logs/ocr_haiku_2026-05-22_v2.log
```

⚠ **啟動前**：先驗證 `~/.claude/.credentials.json` 有 fresh OAuth token（執行任意 Claude command 會 refresh）。Worker 啟動時 load token，過 ~8 小時就過期 → 401 errors。

## 📊 OCR queue 現況（2026-05-22 早上）

- **OCR queue: 183 本待打** （剛 reset 138 本 transient failure 回 queue 後）
- 之前 Haiku 跑了 95 本：OK 1, Failed 58
  - 46 本 "source missing" → 已驗證是 **Drive sync delay** (G:/ 上實際 file EXISTS) → reset 回 queue
  - 49 本 401 token expired → reset 回 queue
  - 23 本 connection error → reset 回 queue
  - 5 本 oversized >1000 pages → 留待 `split_oversized_pdf_by_toc`
  - 內容 Output blocked / content filter → 永久失敗，留 parse_error

## ✅ 上一 session 已驗證 + 處理

1. **OCR source missing 真相確認** — sample 5 file 都 EXISTS（17-22MB normal sizes）
   - 推測：Drive smart-download placeholder 延遲，OCR worker 連續開檔觸發大量 download 排隊
   - **fixable / truly_gone 都是 0** — 沒有真正丟的書

2. **批次 reset 138 本 transient failure**
   - patterns: `Error code: 401`, `Connection error`, `file not found`, `NoneType`
   - 統一改 `parse_error='no extractable text'` 重進 OCR queue
   - 用 `c:/tmp/reset_transient_failures.py`

---

## ✅ 過去 24 小時已完成的工作

### 2026-05-21 我自己跑（commits `59a9138`, `bcb8a9b`, `3314f41`）

1. **v2 oversized resplit** ([scripts/resplit_giant_chunks_v2.py](../../../scripts/resplit_giant_chunks_v2.py))
   - 37/67 books split, +1,468 chunks
   - 30 本 v2 切不動（Schaff Apostolic Fathers / Plato collected works / 中文連續散文）

2. **Sections 切到節級** ([scripts/resplit_to_sections.py](../../../scripts/resplit_to_sections.py))
   - 5 種節級 pattern：h3 / 第N節 / Aquinas 題-條 / Question N / Section N
   - is_already_section_chunk 修正：Aquinas `### 第N題` 內含 `第N節` 可繼續切
   - 28 本 +2,649 chunks
   - **亮點**: Aquinas 神學大全第1冊 +252、第3冊 +288、中國儒學史 +281、舊約背景 +254

3. **歷史學 → 世界宗教/哲學/宗教學 重分類**
   - 468 本歷史學 Haiku judge → **44 本搬出歷史學**
   - 22 本 → 世界宗教（基督教 10 + 伊斯蘭 3 + 猶太 2 + 佛教 2 + 印度 2 + 東亞 1 + 抄本學 1 + 改革史 1）
   - 20 本 → 哲學（含古希臘/古代/中國/倫理學/歷史哲學）
   - 4 本 → 宗教學
   - 2 本 → 文學
   - 1 本 → 神學
   - **user 已自己跑了 apply**（22:45 log: `reorg_history_apply_2026-05-21.log`）

4. **宗師全集 canonical list** ([master-scholars-todo.md](master-scholars-todo.md), 570 行)
   - 7 學科 × 86 學者 × 322 代表作
   - **152/322 已 ingest (47%)**
   - **170 本 ⬜ 待補**
   - 用 OR query cross-check 有 false positive，⬜ 是真實 missing

5. **鹽野七生 Haiku judge** — 證實 EPUB 內 96 chunks 是「海都物語+君士坦丁堡」混排，**無法可靠拆 33 本**

### 2026-05-22 早上

6. **OCR body 失敗救援**
   - 識別 57 本（36 PDF + 21 EPUB）內容 <100 字佔超過 50% — body 沒 extract 出來
   - 21 EPUB 是 `split from set` 套書子卷，跳過
   - **36 本 PDF reset 進 OCR queue** (`UPDATE parse_error='no extractable text'` etc.)
   - 14:00 daily bat 預期接手 → 但 user 命令直接用 Haiku（早一步啟動）

7. **Anna's Archive + z-lib 自動下載失敗**
   - Anna's Archive: DNS 全擋（台灣 ISP）
   - z-library.sk / 1lib.sk: 503
   - singlelogin.re: 200 但 body 空 (Cloudflare TPE bot-detection silent reject)
   - 結論：自動化 download 不可行，需 browser cookies 介入或人手
   - **鹽野七生 33 本 + 170 本宗師待補都暫緩**

---

## 📋 新 session 接續做的事（按優先序）

### 1. ⭐ 啟動 Haiku OCR worker 跑 183 本 queue

```bash
# 確認 credentials 新鮮（user 在 Claude Code 內操作一次）
# 啟動 Haiku worker（background 跑）
"C:\Users\user\AppData\Local\Python\bin\python.exe" -u scripts/ocr_with_gemini.py run --engine haiku 2>&1 | tee scripts/logs/ocr_haiku_2026-05-22_v2.log
```

預期：
- Haiku rate ~6-7 本/小時 (rolling rate limit)
- 跑到 quota 2-strike 自動 pause exit
- 一輪約 5-10 本完成 OCR
- 跑完看 log + queue 剩多少，repeat

### 2. 跑 split_oversized_pdf_by_toc 處理 5 本 >1000 pages

```bash
# 列出 pending_oversized 候選
"C:\Users\user\AppData\Local\Python\bin\python.exe" -u -c "
import os, requests
from dotenv import load_dotenv; load_dotenv(r'C:\Users\user\Desktop\know-graph-lab\.env')
H = {'apikey': os.environ['SUPABASE_SERVICE_ROLE_KEY'], 'Authorization': f'Bearer {os.environ[\"SUPABASE_SERVICE_ROLE_KEY\"]}'}
r = requests.get(f'{os.environ[\"SUPABASE_URL\"]}/rest/v1/ebooks?select=id,title&parse_error=ilike.*pending_oversized*', headers={**H, 'Range':'0-50'}, timeout=30).json()
for row in r: print(row['id'], row['title'])
"

# 對每個 id：
python scripts/split_oversized_pdf_by_toc.py <id> --dry-run
python scripts/split_oversized_pdf_by_toc.py <id>
```

### 3. （仍 pending）Drive smart-download 對抗策略

OCR 跑時遇 source missing 大量 — 是 Drive smart-download placeholder 延遲。可考慮：
- 在跑 OCR 前 prefetch 所有 queue 的 file（先 read 一次強制 download）
- 或在 OCR worker 內加 retry-on-missing 邏輯（等 60s 重試）

### 3. （仍 pending）170 本 ⬜ 宗師書下載

- 自動化路 全擋（Anna's Archive DNS + z-lib Cloudflare）
- 唯二可行方案：
  - **A**. User browser export cookies → 我用 cookies-based scraper
  - **B**. User 開 VPN 後再試自動化
- 暫不動，等 user 指示

### 4. （仍 pending）鹽野七生 33 本

- 同 #3 — 下載被擋。EPUB 結構壞無法靠拆現有 file
- 等 user 開 VPN/cookies 再 download 單本

---

## 📊 整體圖書館 snapshot（2026-05-22 中午 reset 後）

- Total ebooks: ~1,896
- Standardized: ~1,515 (含 sections +28 本)
- **OCR queue: 183 本待打**（138 transient reset + 45 原本）
- Oversized chunks (>400K): **46 本**（v2 + sections 處理後）
- 已 commit/push 進 master 的：v2 script、sections script、master-scholars list、OVERNIGHT report、本 HANDOFF

---

## 🔧 Background scripts (local, gitignored `_*.py`)

存在但不在 git 的：
- [scripts/_yanyo_split_via_gemini.py](../../../scripts/_yanyo_split_via_gemini.py) — yanyo Haiku judge
- [scripts/_reclassify_history_via_gemini.py](../../../scripts/_reclassify_history_via_gemini.py) — 歷史學 reclassify Haiku judge + apply
- [scripts/_master_scholars_cross_check.py](../../../scripts/_master_scholars_cross_check.py) — 宗師清單 cross-check 重生
- [scripts/_yanyo_anna_archive.py](../../../scripts/_yanyo_anna_archive.py) — Anna's Archive scraper（沒用，DNS 被擋）
- [scripts/_zlib_downloader.py](../../../scripts/_zlib_downloader.py) — z-lib SSO scraper（沒用，Cloudflare 擋）
- `c:/tmp/rescue_ocr_body_failed.py` — OCR body 救援 reset script（已跑）

---

## 🗂 重要 path 速查

- Drive: `G:/我的雲端硬碟/資料/電子書/`
- chunks: `G:/我的雲端硬碟/資料/電子書/_chunks/`
- z-lib drop: `z-lib/` (project root)
- logs: `scripts/logs/`
- creds: `~/.claude/.credentials.json` (Haiku OAuth)
- env vars: `.env` (Gemini × 4 keys, ZLIB_EMAIL/PASSWORD, Supabase, R2)
