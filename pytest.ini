[pytest]
# Python-side tests for the ebook transcription / translation pipeline
# (the scripts behind the ebook-pipeline + ebook-translate skills).
# The Vue/Nuxt component tests stay under test/ and run via vitest.
testpaths = scripts/tests
python_files = test_*.py
python_functions = test_*
addopts = -q
