# -*- coding: utf-8 -*-
"""被 OCR 吃掉的節號 —— **寫死的對照表**，每一條都要對得起印出來的書。

交接單 §C 的規矩：補號只認「那一節開頭的原文」，比對不到／比對到多處／位置不在
前後兩節之間，一律擋下不寫。所以這裡不是啟發式，是一張人工核過的查找表。

怎麼判斷一條該不該加：
  1. 切節那支會明講缺哪一號（例如「chapverse：節號被吃掉的節 ['2:3']」）。
  2. 去看那一段的前後文，確認被吃掉的號**確實**在該處起一節。
  3. 能跟外部的標準文本對得上更好（下面每一條都註明了依據）。

🚨 不要因為「補了閘就過」而加條目。閘擋下來的時候，切錯界線與少切一節相比，
   少切一節遠遠比較無害——寧可留著那個 ✗ 讓人看見。
"""
from __future__ import annotations

# 鍵 = 目錄上的作品標題（`apocrypha_toc.py` 解出來的那個）。
# 值 = {節號: 該節開頭的原文}；chapverse 體例用 "章:節" 當鍵。
MISSING: dict[str, dict[int | str, str]] = {
    # 前一輪人工查出來的兩節；本輪由切節那支獨立重新偵測到同樣的 5 與 7，互為佐證。
    "阿拉伯語耶穌嬰孩時期福音": {
        5: "行割禮的時候快到了",
        7: "主耶穌在猶太省的伯利恆出世",
    },
    # PJ 2:3 ＝ 侍女猶辛的回嘴（「我為什麼要…詛咒你呢」）。與標準本的
    # Protevangelium of James 2:3（"Why should I curse you, seeing the Lord
    # has shut up your womb…"）對得上，前一節是 2:2 侍女獻頭帶、後一節是
    # 2:4 亞拿脫下喪服，位置正確。
    "雅各原始福音": {
        "2:3": "「我為什麼要因為你不聽從我就詛咒你呢？",
    },
}


def for_work(title: str) -> dict[int | str, str]:
    return MISSING.get(title, {})
